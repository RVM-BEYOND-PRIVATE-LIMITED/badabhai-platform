The BadaBhai logo — chat-lift mark + Baloo 2 wordmark. 🟠 Placeholder until a real mark exists.

```jsx
<BadaBhaiLogo />                          {/* full lockup */}
<BadaBhaiLogo variant="mark" size={40} /> {/* app icon */}
<BadaBhaiLogo theme="ink" />              {/* on dark surfaces */}
```

- Always one word: BadaBhai. The mark SVG is inlined, so it needs no asset path.
