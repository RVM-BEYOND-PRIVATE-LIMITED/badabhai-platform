Toggle switch — green when on. Use for the worker's safe-field controls (show/hide phone, photo on/off) and alert opt-ins.

```jsx
<Switch defaultChecked label="Show my phone to verified employers" />
```
