Selectable filter pill. Used for trades, skills, languages, and feed filters.

```jsx
<Chip icon="wrench" selected>CNC Operator</Chip>
<Chip onRemove={() => removeFilter('pune')}>Pune</Chip>
```

- `selected` turns it marigold; `onRemove` adds a removable ✕.
