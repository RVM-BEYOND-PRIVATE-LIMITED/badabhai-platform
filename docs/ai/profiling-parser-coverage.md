# Deterministic profiling parser — coverage measurement (POST-#426 / #429)

> **Measurement only. The PR carrying this report changes ZERO runtime files** —
> it touches `tests/` and `docs/` only. Where a fix looks warranted it is written
> down under "Suggested next steps", not implemented.
>
> **Re-measured against `origin/main` AFTER PR #426** (`fea207d`, P1 profiling
> correctness), **#412** (`41d0cb7`, TAX-WELD-1) and **#429** (`64d4001`, issue
> #424 — salary/availability promoted to MUST_ASK). The first revision of this
> report measured commit `6d23419`; those numbers survive only as the labelled
> `pre-#426` column below, never as current behaviour.
>
> **Read finding 7 first** — an ESSENTIAL topic can be marked answered without
> ever being asked, and the completeness signal cannot see it.
>
> Generated by `apps/ai-service/tests/analysis_parser_coverage.py` from the
> synthetic corpus in `apps/ai-service/tests/profiling_answer_corpus.py`.
> Regenerate: `cd apps/ai-service && python tests/analysis_parser_coverage.py --write`.

## What was measured

Each synthetic worker answer is passed to
`app/profiling/signals.py::detect_answered_topics(text, last_asked_topic_id)`
with `last_asked_topic_id` set to the topic that was ASKED — the same call
`app/profiling/interview_engine.py::next_turn` makes (it passes
`st.asked_question_ids[-1]`). A topic counts as ACCEPTED when the asked topic id
is a key in the returned dict; that is exactly the condition under which
`next_turn` appends it to `ConversationState.answered_topics`.

- Fixtures: **265** across **11** topics.
- Human-labelled `accept` (a valid answer): **252**.
- Of those, parser accepted: **160** (**63%** overall).
- Parser gaps (valid answer, not accepted): **92**.
- **Fabrications** (nothing to record, parser stored a VALUE): **0**.
- Denials absorbed (nothing to record, topic marked answered, nothing stored): **2**.

The last two lines were ONE number ("false positives") in the pre-#426 report.
They are split here because #426 changed the meaning: a denial on `education` /
`skills` now marks the ask satisfied with value `None` instead of inventing
`['ITI']`. Reporting both under one heading would have shown an unchanged count
and hidden a real fix.

Zero LLM calls; the path under test is pure regex + gazetteer.


## Findings — this is a POST-#426 / POST-#429 re-measurement

The first revision of this report measured the parser at commit `6d23419`. Three PRs
have landed on it since, and this run re-measures against all three:

- **#426** (`fea207d`, "P1 profiling correctness") fixed four of the defect classes
  this report found — value parsing, negation on capability cues, the overwrite rule,
  and test-time network egress.
- **#412** (`41d0cb7`, TAX-WELD-1) wired welding into the gazetteer, which changes one
  corpus row and the `role` retry wording.
- **#429** (`64d4001`, issue #424, owner ruling) promoted `salary_current` /
  `salary_expected` / `availability` to `MUST_ASK_TOPICS`. This changed the ENGINE,
  not the parser: no acceptance number moves, but the scripted interviews are
  reshaped and the never-asked hazard in finding 3 is narrowed — see **finding 7**,
  which is the more serious form that narrowing exposed.

**The defects are not deleted from this report.** The section "What #426 changed"
keeps the pre-fix value beside the current one, because the record of what was wrong
is half the value of having measured it. What follows separates FIXED from STILL
OPEN, and every verdict is computed from the live parser on each run: **7 of
the 7** recorded defect cases re-measure as fixed.

**Overall acceptance did not move.** #426 fixed what the parser RECORDS, not what it
RECOGNISES: coverage is unchanged at the rate in the table below. Every finding in
sections 1-3 stands exactly as first written.

### 1. Why `CNC` is rejected for `role` — the exact code path (UNCHANGED, still open)

`detect_answered_topics` keys the `role` topic on ONE field:

    if sig.role_id:
        answered["role"] = sig.primary_role
    # app/profiling/signals.py:993-994

`role_id` is set by a first-match-wins scan of the `_ROLES` gazetteer
(`signals.py:716-722`) over the lowercased text. `_ROLES` (`signals.py:79-88`)
contains eight keywords:

    cam programmer · programmer · setter · vmc · hmc · grinding · turner · turning

There is **no `cnc` keyword and no `operator` keyword**. So `"CNC"` sets no
`role_id` and the topic is not marked. `"CNC"` is also absent from `_MACHINES`
(`signals.py:27-36`, which has `cnc lathe` but no bare `cnc`), so the answer falls
through BOTH tables and `detect_answered_topics("CNC", "role")` returns `{}` —
nothing at all is recorded from it.

The worse variant is `"CNC operator"`, the single most likely thing a worker types.
`operator` is not a role keyword, but it IS an operation-knowledge cue
(`signals.py:744-746`), which appends `"machine operation"` to `sig.skills`, which
makes `detect_answered_topics` mark **`skills`** answered (`signals.py:1001-1002`).
So the canonical answer to the role question:

- leaves `role` unanswered → it is re-asked once (`MAX_ASKS_PER_TOPIC = 2`,
  `interview_engine.py:50`) and then abandoned;
- silently marks `skills` answered with `["machine operation"]` — and
  `_next_topic` never returns an already-answered topic
  (`interview_engine.py:490/496/501`), so **the skills question is never asked**.

Both effects are visible in scripted interview A below. #426 did not touch the
gazetteer, the question bank or topic ordering, so this is exactly as first measured.

### 2. The conflation hypothesis — CONFIRMED (UNCHANGED, still open)

The shipped question (`app/profiling/question_bank.py:80`) is:

> Aap kaunsa kaam karte hain — CNC, VMC, HMC operator, setter ya programmer?

It offers a five-item list that mixes two different dimensions: **machine type**
(CNC, VMC, HMC) and **job function** (operator, setter, programmer). Asking
"kaunsa kaam" (which work) and then listing machine types invites a machine-type
answer.

Measured against the parser, each option resolves `role` as follows
(`test_role_question_offers_options_the_parser_cannot_resolve`):

| option offered | dimension | resolves `role`? |
| --- | --- | :---: |
| CNC | machine type | **NO** |
| VMC | machine type | yes (→ "VMC Operator") |
| HMC | machine type | yes (→ "HMC Operator") |
| operator | job function | **NO** |
| setter | job function | yes |
| programmer | job function | yes |

So the conflation is not only in the question — it is baked into the gazetteer,
inconsistently. Two of the three machine types (`vmc`, `hmc`) are stored AS roles,
mapping to "VMC Operator"/"HMC Operator"; the third (`cnc`) is not. And of the
three job functions, the most generic one (`operator`) is the one that fails.

**Two of the six options the question puts in the worker's mouth cannot be
parsed** — and they are the two a worker is most likely to repeat back, because
"CNC" is the first word in the list and "operator" is the word most workers use
for their own job.

The retry wording still does not rescue this, and #412 widened it rather than fixing
it. `role.retry_question` (`question_bank.py:90-92`) is now "Machine ya kaam ke naam
se bataiye — VMC operator, CNC turner, setter, programmer ya welder?" — it drops
"CNC" and bare "operator" and adds "welder", and every option it offers does resolve.
The parseable prompt still only arrives on the SECOND attempt, after the honest first
answer was silently dropped.

### 3. Dead topics (UNCHANGED) — but see finding 7, which grew out of this

**None.** All 11 topics are satisfied by at least one plausible answer
(`test_no_topic_is_structurally_dead`). The failure mode measured here is partial
coverage, not structural deadness. Two adjacent hazards are real, though:

- **auto-closed topics** — `skills` is marked answered by 15 of the
  48 role/machine answers without ever being asked, so in practice
  it is frequently a topic the worker never sees;
- **early wrap-up** — the engine stops as soon as `_extraction_ready` holds, so a
  worker whose essentials land in the first few turns is never asked about the rest.

**#429 narrowed the second one.** Promoting `salary_current` / `salary_expected` /
`availability` to `MUST_ASK_TOPICS` means `_extraction_ready` now also requires those
to have been ASKED, so the wrap-up can no longer skip money and notice period.
Measured on scripted interview B: `never_asked` was seven topics before #429 and is
`['machines', 'skills', 'education']` now.

It did NOT close the hazard, it moved it — see finding 7, which is the more serious
form and is the reason this revision exists.

### 4. A later answer OVERWRITES an already-collected value — **FIXED by #426**

Found while measuring, not part of the original brief. `next_turn` guarded the
ANSWERED list but not the COLLECTED map, so any later message that re-triggered a
detector overwrote the earlier value, last-mention-wins. A worker answered
`experience` with "10 saal"; their answer to the LAST question ("ITI + 3 saal
apprenticeship") reset `collected["experience"]` to `3.0`. A ten-year machinist
shipped as a three-year one, with no signal that anything was replaced.

`interview_engine._may_commit` (`interview_engine.py:113-150`) now gates the write:
the topic BEING ASKED always commits; an explicit correction
(`signals.is_correction`) always commits; otherwise **first write wins** — an
incidental cross-topic signal may fill an EMPTY slot but never overwrite an
established one. Scripted interview C below is the regression guard: the same script
that used to end at `experience = 3.0` now ends at `10.0`.

**Residual, measured here, not fixed by #426:** the correction marker is scoped to
the whole MESSAGE, not to a span. Interview D is interview C with "nahi nahi," in
front of the education answer — the correction is aimed at `education`, but it
unlocks the incidental `experience` overwrite riding along in the same sentence, and
experience drops back to `3.0`. A worker correcting one field can still silently
rewrite another.

### 5. Negation — **CAPABILITY cues fixed by #426, VALUE cues STILL OPEN**

Originally: negation was invisible everywhere, so a denial asserted its own opposite
("iti nahi kiya" → `education=['ITI']`).

`signals._apply_negation` now blanks a negated span — a 3-word
BACKWARD window from the negator, clamped to the clause, because Hindi/Hinglish puts
the negator AFTER what it negates — before the cue tables run. Backward-only is
deliberate: a forward window would eat the contrast in "CNC nahi, VMC karta hu",
which is the value the worker IS asserting. For `education` and `skills` a denial now
also ANSWERS the ask with value `None` (`signals._NEGATION_ANSWERS_TOPICS`): the topic
is marked answered, nothing is collected, and it is not mistaken for silence.

**Measured verdict on the 16 probes below: 0 of the 8 CAPABILITY probes are still open, and 6 of the 8 VALUE probes are.**

The masking is applied ONLY to the capability cue families (role / machines /
controllers / skills / knowledge / education). `signals.detect` states the exclusion
in code: location, availability, salary and experience deliberately keep reading the
ORIGINAL text, because masking them cost real answers in the first measurement
("Pune se bahar nahi jaunga" loses Pune) and their negation was out of #426's scope.

So the parser is **still recording the opposite of what the worker said** for those
four topics:

| the worker says | the profile records |
| --- | --- |
| `Pune nahi, Delhi mein hu` | current_location **Pune** (+ Delhi as a *preference*) |
| `Pune nahi jaunga` | preferred_locations **['Pune']** |
| `abhi turant nahi, 1 mahina lagega` | availability **immediate** |
| `22000 nahi milta` | salary_current **22000** |
| `25000 nahi chahiye, 30000 chahiye` | salary_expected **25000** — the figure REFUSED |
| `2 saal nahi hua abhi` | experience **2.0** |

This is a **known, disclosed, OPEN gap**, not a fixed one, and it is the largest
remaining correctness item on this parser: unlike a coverage gap (which leaves a
field empty and re-askable) a negation miss writes a confidently wrong value onto a
worker's resume. See "Suggested next steps" for the shape of a fix — this PR
deliberately implements nothing.

### 6. New findings this re-run exposed

1. **`welding machine` closes `role` and `skills` while leaving `machines` open.**
   Asked "kaunsi machine", the answer "welding machine" is not in `_MACHINES`, so the
   asked topic stays unanswered — but the TAX-WELD-1 path reads it as `role=Welder`,
   `skills=['welding']`. Attributed to **#412, not #426**. For a worker in the
   CNC/VMC family this fills `role` from a MACHINE answer, and under the new
   first-write-wins rule that value then sticks unless `role` was already set.
2. **`kabhi` was read as `abhi` — FIXED by the #424 follow-up.** The availability cue
   used to be a plain substring test, so "kabhi" (ever), "kabhi kabhi" (occasionally)
   and "kabhi bhi" (whenever) all set `availability = immediate`; so did every answer
   to the bank's own "**Abhi** kis sheher mein hain?" / "**Abhi** salary kitni hai?".
   The cue family is now word-boundary matched and requires a GENUINE availability cue
   (join/start intent, being free, a notice duration) — a bare time adverb only counts
   next to a join intent. "vmc nahi chalaya kabhi" no longer marks availability.
   Pre-existing and NOT caused by #426; surfaced by probing for the negation gap and
   fixed once #429 made `availability` a MUST_ASK topic that the false positive was
   silently satisfying.
3. **`salary_expected` stores the refused number.** "25000 nahi chahiye, 30000
   chahiye" records 25000: the parser is both first-number-wins and negation-blind,
   and the two compose into the worst available answer.
4. **The correction marker is message-scoped** — see the residual in section 4.

### 7. An ESSENTIAL topic can be marked answered WITHOUT EVER BEING ASKED

**This is the most serious finding in this report.** It surfaced while re-measuring
after #429 and it is not the hazard finding 3 originally described — it is a worse
one that finding 3's wording ("salary, availability, controllers or education") had
been quietly standing in front of.

`_extraction_ready` gates the four `ESSENTIAL_TOPICS` on **answered**, never on
**asked**:

    if not all(t in st.answered_topics for t in ESSENTIAL_TOPICS):
        return False
    # app/profiling/interview_engine.py:113-114

and `_unanswered_essentials` — the EXPLICIT completeness signal, the thing that is
supposed to declare an incomplete profile — is computed the same way:

    return [t for t in ESSENTIAL_TOPICS if t not in st.answered_topics]

Neither looks at `asked_question_ids`. But `detect_answered_topics` marks topics from
CROSS-TOPIC inference, so an essential can be closed by a DIFFERENT question's
answer, and `_next_topic` then never returns it because it is already in
`answered_topics`.

**Measured on scripted interview B** (a worker whose phrasing matches the gazetteer):

- `extraction_ready` = **True**
- `unanswered_essentials` = **`[]`** — the profile reports itself COMPLETE
- yet `machines`, an ESSENTIAL topic, is in `never_asked`

The worker was asked "aap kaunsa kaam karte hain?" and said "VMC operator". The
gazetteer read "vmc" out of that ONE answer and filled `machines=['VMC']`. **The
machine question was never put to them.** A worker who runs a VMC *and* an HMC *and*
a lathe ships as VMC-only, and every completeness signal the system has says nothing
is missing.

This is not confined to `machines`. Across the corpus, each essential is closed by
some OTHER topic's answer in:

| essential topic | closed by another topic's answer | example |
| --- | ---: | --- |
| `role` | 12 fixtures | `machines` answer `VMC` |
| `machines` | 5 fixtures | `role` answer `VMC operator` |
| `experience` | 1 fixture | `education` answer `ITI + 3 saal apprenticeship` |
| `current_location` | 1 fixture | `preferred_locations` answer `abhi Pune mein hu, Delhi bhi chalega` |

`role` is the largest: 12 fixtures for other topics close it — mostly
`machines` answers, because the gazetteer stores `vmc`/`hmc` as ROLES as well as
machines (finding 2). `role` happens to be asked first in bank order, so it is asked
before anything can pre-empt it; that is ORDERING luck, not a guarantee.

**Why #429 does not cover this.** #429 fixed exactly this shape of hole for the
MUST_ASK topics, and it fixed it with an **asked-or-answered** gate. The essentials
kept the answered-only gate — reasonably, since an essential must genuinely be
ANSWERED — but the consequence is that inference satisfies them while the question
goes unasked. Measured across all four scripted interviews below:

- `MUST_ASK_TOPICS` never asked: **none — the #429 gate holds**
- `ESSENTIAL_TOPICS` never asked: **['machines']**

Written up, NOT fixed here — this PR changes zero runtime files. The shape of a fix
is in "Suggested next steps" item 0.

## What #426 changed — measured before / after

`pre-#426` is what THIS harness recorded at commit `6d23419`: a static record of
a measurement that was taken, kept deliberately so the report still shows what
was wrong. `now` and the verdict are recomputed live on every run — a verdict of
FIXED is measured, never asserted, and a later regression flips it back to OPEN.

| asked topic | worker answer | pre-#426 | now | want | verdict |
| --- | --- | --- | --- | --- | --- |
| `experience` | `2.5 saal` | `5.0` | `2.5` | `2.5` | **FIXED** |
| `salary_current` | `1.5 lakh saal ka` | `150000` | `12500` | `12500` | **FIXED** |
| `salary_current` | `2012 se kaam kar raha hu` | `2012` | `_topic not marked_` | `_topic not marked_` | **FIXED** |
| `experience` | `22000 salary milti hai` | `0.0` | `_topic not marked_` | `_topic not marked_` | **FIXED** |
| `education` | `iti nahi kiya, kaam se hi seekha` | `['ITI']` | `None` | `None` | **FIXED** |
| `education` | `diploma nahi hai` | `['Diploma']` | `None` | `None` | **FIXED** |
| `skills` | `setting nahi aati, sirf chalata hu` | `['machine operation', 'basic setting']` | `['machine operation']` | `['machine operation']` | **FIXED** |

- `2.5 saal` — P1-3: the years regex had no left boundary and matched the decimal's 2nd digit
- `1.5 lakh saal ka` — P1-3: an ANNUAL figure was stored as a MONTHLY one
- `2012 se kaam kar raha hu` — P1-3: a bare 1900-2099 year is only money when the text says money
- `22000 salary milti hai` — P1-3: 'sal' matched inside 'salary'; the added \b stops it
- `iti nahi kiya, kaam se hi seekha` — P1-2: the denial no longer asserts its opposite; topic answered, nothing stored
- `diploma nahi hai` — P1-2: same — a denial IS a complete answer to the education question
- `setting nahi aati, sirf chalata hu` — P1-2: 'basic setting' is gone; the asserted 'chalata hu' survives

## Negation — fixed on CAPABILITY cues, STILL OPEN on VALUE cues

A row is OPEN when the parser still records the very token the worker DENIED.
The predicate is crude on purpose (does the denied token appear in what was
stored?) so the verdict cannot be talked into being green.

| cue family | asked topic | worker answer | denied | recorded now | verdict |
| --- | --- | --- | --- | --- | --- |
| CAPABILITY | `education` | `iti nahi kiya, kaam se hi seekha` | `ITI` | `None` | honoured |
| CAPABILITY | `education` | `diploma nahi hai` | `Diploma` | `None` | honoured |
| CAPABILITY | `skills` | `setting nahi aati, sirf chalata hu` | `basic setting` | `['machine operation']` | honoured |
| CAPABILITY | `skills` | `program edit nahi aata` | `program editing` | `None` | honoured |
| CAPABILITY | `role` | `setter nahi hu` | `Setter` | `_topic not marked_` | honoured |
| CAPABILITY | `role` | `CNC nahi, VMC karta hu` | `CNC` | `VMC Operator` | honoured |
| CAPABILITY | `machines` | `vmc nahi chalaya kabhi` | `VMC` | `_topic not marked_` | honoured |
| CAPABILITY | `controllers` | `fanuc nahi, siemens hai` | `Fanuc` | `['Siemens']` | honoured |
| VALUE | `current_location` | `Pune nahi, Delhi mein hu` | `Pune` | `Pune` | **OPEN** |
| VALUE | `current_location` | `Pune mein nahi rehta` | `Pune` | `Pune` | **OPEN** |
| VALUE | `preferred_locations` | `Pune nahi jaunga` | `Pune` | `['Pune']` | **OPEN** |
| VALUE | `availability` | `abhi turant nahi, 1 mahina lagega` | `immediate` | `notice_period` | honoured |
| VALUE | `availability` | `turant nahi aa sakta` | `immediate` | `_topic not marked_` | honoured |
| VALUE | `salary_current` | `22000 nahi milta` | `22000` | `22000` | **OPEN** |
| VALUE | `salary_expected` | `25000 nahi chahiye, 30000 chahiye` | `25000` | `25000` | **OPEN** |
| VALUE | `experience` | `2 saal nahi hua abhi` | `2.0` | `2.0` | **OPEN** |

- `setting nahi aati, sirf chalata hu` — the contrastive positive 'chalata hu' is kept
- `CNC nahi, VMC karta hu` — backward-only window: the contrast VMC is preserved, which is the point
- `vmc nahi chalaya kabhi` — `machines` correctly not marked — but see the cross-topic note below
- `Pune nahi, Delhi mein hu` — records the city the worker LEFT — and files Delhi as a PREFERENCE
- `Pune nahi jaunga` — records a refusal as a preference
- `abhi turant nahi, 1 mahina lagega` — records the OPPOSITE of what the worker said
- `25000 nahi chahiye, 30000 chahiye` — first-number-wins AND negation-blind: it stores the figure they REFUSED

## Findings this re-run exposed (no corpus fixture covers these)

| asked topic | worker answer | full detector output | note |
| --- | --- | --- | --- |
| `machines` | `welding machine` | `role`=Welder, `skills`=['welding'] | `machines` NOT marked, but `role`+`skills` are closed on a welding read |
| `machines` | `kabhi` | _nothing_ | FIXED (#424 follow-up): 'abhi' was substring-matched inside 'kabhi'; the cue is now word-boundary matched AND needs a real availability cue |
| `machines` | `kabhi kabhi` | _nothing_ | FIXED (#424 follow-up): 'sometimes' no longer reads as 'available immediately' |
| `availability` | `kabhi bhi` | `availability`=immediate | 'whenever' — still immediate, now for the RIGHT reason: matched explicitly as an anytime phrase, and only because the availability question was the one asked |

## Per-topic acceptance

| topic | fixtures | should accept | accepted | acceptance rate | gap count |
| --- | ---: | ---: | ---: | ---: | ---: |
| `role` | 24 | 23 | 8 | 35% | 15 |
| `machines` | 24 | 23 | 14 | 61% | 9 |
| `experience` | 25 | 24 | 12 | 50% | 12 |
| `skills` | 24 | 23 | 14 | 61% | 9 |
| `current_location` | 24 | 23 | 16 | 70% | 7 |
| `preferred_locations` | 24 | 23 | 17 | 74% | 6 |
| `controllers` | 24 | 22 | 13 | 59% | 9 |
| `salary_current` | 24 | 23 | 19 | 83% | 4 |
| `salary_expected` | 24 | 23 | 14 | 61% | 9 |
| `availability` | 24 | 24 | 22 | 92% | 2 |
| `education` | 24 | 21 | 11 | 52% | 10 |

## Topics ranked by gap size

| rank | topic | gaps | acceptance rate | essential? |
| ---: | --- | ---: | ---: | --- |
| 1 | `role` | 15 | 35% | **ESSENTIAL** |
| 2 | `experience` | 12 | 50% | **ESSENTIAL** |
| 3 | `education` | 10 | 52% | optional |
| 4 | `controllers` | 9 | 59% | optional |
| 5 | `machines` | 9 | 61% | **ESSENTIAL** |
| 6 | `salary_expected` | 9 | 61% | optional |
| 7 | `skills` | 9 | 61% | optional |
| 8 | `current_location` | 7 | 70% | **ESSENTIAL** |
| 9 | `preferred_locations` | 6 | 74% | MUST_ASK |
| 10 | `salary_current` | 4 | 83% | optional |
| 11 | `availability` | 2 | 92% | optional |

## Rejected answers a human would call valid (the parser gaps)

### `role` — 15 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `CNC` | english | _nothing_ | verbatim first option in the question |
| `CNC operator` | hinglish | `skills` | the canonical shop-floor answer |
| `lathe operator` | hinglish | `machines`, `skills` |  |
| `operator` | english | `skills` | verbatim option in the question |
| `machine operator hu` | hinglish | `skills` |  |
| `CNC machine chalata hoon` | hinglish | `skills` |  |
| `main CNC operator ka kaam karta hu` | hinglish | `skills` |  |
| `cnc oprator` | misspelling | _nothing_ |  |
| `seter ka kaam` | misspelling | _nothing_ |  |
| `V M C operator` | spaced | `skills` |  |
| `मैं वीएमसी ऑपरेटर हूँ` | devanagari | _nothing_ |  |
| `प्रोग्रामर` | devanagari | _nothing_ |  |
| `helper hu, machine seekh raha hu` | hinglish | _nothing_ | real shop-floor role, out of the CNC/VMC gazetteer |
| `fitter` | english | _nothing_ | adjacent trade |
| `supervisor` | english | _nothing_ | adjacent role |

### `machines` — 9 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `cnc` | english | _nothing_ | verbatim shorthand workers use |
| `milling machine` | english | _nothing_ |  |
| `drilling machine` | english | _nothing_ |  |
| `वीएमसी` | devanagari | _nothing_ |  |
| `vtl` | english | _nothing_ | vertical turret lathe |
| `boring machine` | english | _nothing_ |  |
| `shaper machine` | english | _nothing_ |  |
| `power press` | english | _nothing_ | out-of-family machine |
| `welding machine` | english | `role`, `skills` | out-of-family machine |

### `experience` — 12 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `char saal` | hinglish | _nothing_ | numeral as a word |
| `chaar saal ka experience hai` | hinglish | _nothing_ |  |
| `do saal` | hinglish | _nothing_ | numeral as a word |
| `teen saal` | hinglish | _nothing_ | numeral as a word |
| `ek saal` | hinglish | _nothing_ | numeral as a word |
| `bees saal` | hinglish | _nothing_ | numeral as a word |
| `6 mahine` | hinglish | _nothing_ | months, not years |
| `5 साल` | devanagari | _nothing_ |  |
| `पाँच साल का तजुर्बा` | devanagari | _nothing_ |  |
| `2012 se kaam kar raha hu` | hinglish | _nothing_ | start year |
| `fresher hu` | hinglish | _nothing_ | zero experience is a value |
| `naya hu, abhi start kiya` | hinglish | _nothing_ |  |

### `skills` — 9 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `sab aata hai` | hinglish | _nothing_ | blanket yes |
| `sirf operation` | hinglish | _nothing_ |  |
| `vernier aur micrometer use karta hu` | hinglish | _nothing_ | inspection is a skill a human would record |
| `quality checking karta hu` | hinglish | _nothing_ |  |
| `sirf loading unloading` | hinglish | _nothing_ |  |
| `deburring aur finishing` | english | _nothing_ |  |
| `बेसिक सेटिंग आती है` | devanagari | _nothing_ |  |
| `ड्रॉइंग पढ़ लेता हूँ` | devanagari | _nothing_ |  |
| `counter boring aur tapping` | english | _nothing_ |  |

### `current_location` — 7 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `Chakan` | english | _nothing_ | Pune auto belt |
| `Ranjangaon` | english | _nothing_ | MIDC industrial area |
| `Bhiwadi` | english | _nothing_ | Rajasthan industrial belt |
| `Jamshedpur` | english | _nothing_ |  |
| `Kolhapur` | english | _nothing_ | foundry cluster |
| `Bihar` | hinglish | _nothing_ | state-only answer |
| `पुणे` | devanagari | _nothing_ |  |

### `preferred_locations` — 6 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `apne sheher mein hi` | hinglish | _nothing_ | wants to stay local — a real preference |
| `ghar ke paas hi chahiye` | hinglish | _nothing_ |  |
| `Gujarat mein` | hinglish | _nothing_ | state-level preference |
| `South India` | english | _nothing_ |  |
| `NCR` | english | _nothing_ |  |
| `जहाँ भी काम मिले` | devanagari | _nothing_ |  |

### `controllers` — 9 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `fanuk` | misspelling | _nothing_ |  |
| `phanuc` | misspelling | _nothing_ |  |
| `फैनुक` | devanagari | _nothing_ |  |
| `sinumerik` | english | _nothing_ | Siemens control, brand name |
| `Mazatrol` | english | _nothing_ | Mazak control |
| `Syntec` | english | _nothing_ |  |
| `GSK` | english | _nothing_ |  |
| `Hurco` | english | _nothing_ |  |
| `Delta controller` | english | _nothing_ |  |

### `salary_current` — 4 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `15 hazaar` | hinglish | _nothing_ | the commoner spelling |
| `daily 700 rupaye` | hinglish | _nothing_ | daily wage |
| `pandrah hazaar` | hinglish | _nothing_ | numeral as a word |
| `पंद्रह हज़ार` | devanagari | _nothing_ |  |

### `salary_expected` — 9 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `25 hazaar chahiye` | hinglish | _nothing_ |  |
| `double chahiye` | hinglish | _nothing_ | relative demand |
| `jo aap theek samjhe` | hinglish | _nothing_ | deferential |
| `aapke hisab se` | hinglish | _nothing_ | deferential |
| `company jo de` | hinglish | _nothing_ | deferential |
| `jitna aap de sako` | hinglish | _nothing_ | deferential |
| `salary aapki marzi` | hinglish | _nothing_ | deferential |
| `negotiable` | english | _nothing_ |  |
| `जो भी मिले` | devanagari | _nothing_ |  |

### `availability` — 2 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `जल्दी जॉइन कर सकता हूँ` | devanagari | _nothing_ |  |
| `पंद्रह दिन` | devanagari | _nothing_ |  |

### `education` — 10 gap(s)

| worker answer | register | detected instead | note |
| --- | --- | --- | --- |
| `B.E. mechanical` | english | _nothing_ |  |
| `10th pass` | english | _nothing_ |  |
| `12th pass` | english | _nothing_ |  |
| `8th pass` | english | _nothing_ |  |
| `graduation kiya hai` | hinglish | _nothing_ |  |
| `BA pass` | english | _nothing_ |  |
| `apprentice kiya tha` | hinglish | _nothing_ |  |
| `CNC ka course kiya private institute se` | hinglish | _nothing_ |  |
| `आईटीआई किया है` | devanagari | _nothing_ |  |
| `डिप्लोमा किया है` | devanagari | _nothing_ |  |

## Fabrications — nothing to record, but the parser stored a VALUE

The most dangerous class: `interview_engine._next_topic` never returns a topic
already in `answered_topics`, so the topic is closed for the rest of the
interview, the real value is never collected, and an invented one ships.

**None in this corpus.** The pre-#426 report listed two —
`iti nahi kiya, kaam se hi seekha` → `education=['ITI']` and
`diploma nahi hai` → `education=['Diploma']`. Both are now in the table
below instead: the topic is still closed, but nothing is written.

## Denials absorbed — ask satisfied, nothing stored

Post-#426 designed behaviour (`signals._NEGATION_ANSWERS_TOPICS`) for the two
topics where "no" is itself a complete answer. The topic is marked answered with
value `None`, so it is not re-asked and not mistaken for silence, and nothing
reaches `collected`. Listed rather than folded into a headline number so the
closing of the ask stays visible and reviewable.

| asked topic | worker answer | recorded | note |
| --- | --- | --- | --- |
| `education` | `iti nahi kiya, kaam se hi seekha` | `None` | negated ITI |
| `education` | `diploma nahi hai` | `None` | negated diploma |

## Cross-topic marks — answer accepted for a topic that was NOT asked

Same mechanism as above, but triggered by a legitimate answer. A cross-topic
mark is only safe when the worker really did volunteer that other topic.

| asked topic | also marked | occurrences |
| --- | --- | ---: |
| `role` | `skills` | 13 |
| `machines` | `role` | 11 |
| `role` | `machines` | 5 |
| `machines` | `skills` | 2 |
| `current_location` | `preferred_locations` | 1 |
| `education` | `experience` | 1 |
| `education` | `role` | 1 |
| `preferred_locations` | `current_location` | 1 |
| `role` | `availability` | 1 |

### The dangerous subset: answer accepted ONLY for another topic

The asked topic stays unanswered (so it is re-asked, then abandoned) **and** an
unrelated topic is silently closed on evidence the worker never gave.

| asked topic | worker answer | marked instead |
| --- | --- | --- |
| `role` | `CNC operator` | `skills`=['machine operation'] |
| `role` | `lathe operator` | `machines`=['CNC Lathe'], `skills`=['machine operation'] |
| `role` | `operator` | `skills`=['machine operation'] |
| `role` | `machine operator hu` | `skills`=['machine operation'] |
| `role` | `CNC machine chalata hoon` | `skills`=['machine operation'] |
| `role` | `main CNC operator ka kaam karta hu` | `skills`=['machine operation'] |
| `role` | `V M C operator` | `skills`=['machine operation'] |
| `machines` | `welding machine` | `role`=Welder, `skills`=['welding'] |

## Accepted, but is the recorded VALUE right?

Acceptance is not correctness. These answers DO mark their topic answered —
they are counted as successes in the table above — but the value stored in
`ConversationState.collected` may not be what the sentence means. A wrong value
is worse than a miss: it is never re-asked and it flows into the profile/resume.

`status` is computed live: **ok** when the parser matches the human-correct
value, **WRONG** when it does not, and `n/a` where the correct answer is a range
or a relative demand the field cannot hold at all — open by construction, and a
contract question rather than a parser bug.

| asked topic | worker answer | recorded value | a human would record | status |
| --- | --- | --- | --- | --- |
| `experience` | `2.5 saal` | `2.5` | 2.5 years | ok |
| `experience` | `2 saal 6 mahine` | `2.0` | 2.5 years | **WRONG** |
| `education` | `ITI + 3 saal apprenticeship` | `['ITI']` | education only; not 3 years of experience | ok |
| `salary_current` | `1.5 lakh saal ka` | `12500` | an ANNUAL 1.5L read as a monthly figure | ok |
| `salary_current` | `18 se 20 hazar` | `20000` | a range 18k-20k | n/a — field cannot hold it |
| `salary_expected` | `abhi se 5000 zyada` | `5000` | current + 5000, not 5000 | n/a — field cannot hold it |
| `salary_expected` | `30-35k` | `35000` | a range 30k-35k | n/a — field cannot hold it |
| `skills` | `setting nahi aati, sirf chalata hu` | `['machine operation']` | operation only — setting explicitly NOT known | ok |
| `preferred_locations` | `Pune, Chakan, Ranjangaon` | `['Pune']` | three locations | n/a — field cannot hold it |
| `current_location` | `Peenya, Bangalore` | `Peenya` | one city (Bangalore), area = Peenya | **WRONG** |
| `current_location` | `Noida sector 63` | `Noida` | Noida | ok |

## Dead-topic check (the evidence behind finding 3)

**None.** Every one of the 11 topics is satisfied by at least one fixture in
this corpus, so no topic is structurally unanswerable. The failure mode is
PARTIAL coverage (the rates above), not a dead topic.

## Engine-level consequence (scripted interviews, mock mode)

Every run drives the real `interview_engine.next_turn` with no network. The
script answers whichever topic the engine asks.

### A — plausible worker, answers in the registers the parser does not cover

| engine asked | worker replied |
| --- | --- |
| `role` | `CNC operator` |
| `role` | `CNC operator` |
| `machines` | `cnc` |
| `machines` | `cnc` |
| `experience` | `char saal` |
| `experience` | `char saal` |
| `current_location` | `Chakan` |
| `current_location` | `Chakan` |
| `preferred_locations` | `ghar ke paas hi chahiye` |
| `controllers` | `fanuk` |
| `salary_current` | `15 hazaar` |
| `salary_expected` | `jo aap theek samjhe` |
| `availability` | `do mahine baad` |
| `education` | `10th pass` |

- turns: **15**
- extraction_ready: **True**
- answered topics: `['skills', 'availability']`
- **unanswered essentials: `['role', 'machines', 'experience', 'current_location']`**
- never asked at all: `['skills']`
- collected: `{'skills': ['machine operation'], 'availability': 'notice_period'}`

### B — worker whose phrasing happens to match the gazetteer

| engine asked | worker replied |
| --- | --- |
| `role` | `VMC operator` |
| `experience` | `4 saal` |
| `current_location` | `Pune` |
| `preferred_locations` | `kahin bhi chalega` |
| `controllers` | `Fanuc` |
| `salary_current` | `22000` |
| `salary_expected` | `30k chahiye` |
| `availability` | `15 din` |

- turns: **9**
- extraction_ready: **True**
- answered topics: `['role', 'machines', 'skills', 'experience', 'current_location', 'preferred_locations', 'controllers', 'salary_current', 'salary_expected', 'availability']`
- **unanswered essentials: `[]`**
- never asked at all: `['machines', 'skills', 'education']`
- **ESSENTIAL topics NEVER ASKED (finding 7): `['machines']`** — closed by inference from another answer, and invisible to `unanswered_essentials` above
- collected: `{'role': 'VMC Operator', 'machines': ['VMC'], 'skills': ['machine operation'], 'experience': 4.0, 'current_location': 'Pune', 'preferred_locations': 'flexible', 'controllers': ['Fanuc'], 'salary_current': 22000, 'salary_expected': 30000, 'availability': 'notice_period'}`

### C — the overwrite rule holds: `experience` survives the education answer (pre-#426 this ended at `3.0`)

| engine asked | worker replied |
| --- | --- |
| `role` | `VMC operator` |
| `experience` | `10 saal` |
| `current_location` | `gaon mein` |
| `current_location` | `gaon mein` |
| `preferred_locations` | `kahin bhi chalega` |
| `controllers` | `Fanuc` |
| `salary_current` | `22000` |
| `salary_expected` | `30k chahiye` |
| `availability` | `15 din` |
| `education` | `ITI + 3 saal apprenticeship` |

- turns: **11**
- extraction_ready: **True**
- answered topics: `['role', 'machines', 'skills', 'experience', 'preferred_locations', 'controllers', 'salary_current', 'salary_expected', 'availability', 'education']`
- **unanswered essentials: `['current_location']`**
- never asked at all: `['machines', 'skills']`
- **ESSENTIAL topics NEVER ASKED (finding 7): `['machines']`** — closed by inference from another answer, and invisible to `unanswered_essentials` above
- collected: `{'role': 'VMC Operator', 'machines': ['VMC'], 'skills': ['machine operation'], 'experience': 10.0, 'preferred_locations': 'flexible', 'controllers': ['Fanuc'], 'salary_current': 22000, 'salary_expected': 30000, 'availability': 'notice_period', 'education': ['ITI']}`

### D — the other half of the rule: an EXPLICIT correction still commits — and, because the marker is message-scoped, it drags `experience` down with it

| engine asked | worker replied |
| --- | --- |
| `role` | `VMC operator` |
| `experience` | `10 saal` |
| `current_location` | `gaon mein` |
| `current_location` | `gaon mein` |
| `preferred_locations` | `kahin bhi chalega` |
| `controllers` | `Fanuc` |
| `salary_current` | `22000` |
| `salary_expected` | `30k chahiye` |
| `availability` | `15 din` |
| `education` | `nahi nahi, ITI + 3 saal apprenticeship` |

- turns: **11**
- extraction_ready: **True**
- answered topics: `['role', 'machines', 'skills', 'experience', 'preferred_locations', 'controllers', 'salary_current', 'salary_expected', 'availability', 'education']`
- **unanswered essentials: `['current_location']`**
- never asked at all: `['machines', 'skills']`
- **ESSENTIAL topics NEVER ASKED (finding 7): `['machines']`** — closed by inference from another answer, and invisible to `unanswered_essentials` above
- collected: `{'role': 'VMC Operator', 'machines': ['VMC'], 'skills': ['machine operation'], 'experience': 3.0, 'preferred_locations': 'flexible', 'controllers': ['Fanuc'], 'salary_current': 22000, 'salary_expected': 30000, 'availability': 'notice_period', 'education': ['ITI']}`

## Suggested next steps — WRITTEN DOWN, NOT IMPLEMENTED

This PR is measurement-only and changes zero runtime files. The items below are
the shape of a fix, in the order the data ranks them. None of them is done here.

0. **An ESSENTIAL topic marked answered without ever being asked** (finding 7)
   — ranked above everything else because it defeats the completeness signal
   itself: `unanswered_essentials` reports `[]` while `machines` was never put
   to the worker. #429 already established the pattern for a fix — it gave the
   MUST_ASK topics an **asked-or-answered** gate. The essentials cannot simply
   copy that (an essential must be genuinely ANSWERED, and asked-or-answered
   would WEAKEN them). Two directions worth the owner's call, neither
   implemented here:
   - **require asked AND answered for essentials** — strictly stronger, but it
     costs turns and could re-ask something the worker already volunteered;
   - **keep the inference but confirm it** — ask the question with the inferred
     value pre-filled ("aap VMC chalate hain — aur koi machine?"), which keeps
     the turn count and still gives the worker the chance to correct/extend.
   Whichever is chosen, `unanswered_essentials` should probably distinguish
   "answered by the worker" from "inferred and never confirmed" — today it
   cannot, and that is what makes this silent.
1. **Negation on VALUE cues** (finding 5) — the only class that writes a
   confidently WRONG value rather than leaving a field empty. `_apply_negation`
   already produces the masked text; the work is deciding, per topic, whether a
   denial should suppress-and-re-ask (location, salary, experience) or suppress
   and take the contrast (`Pune nahi, Delhi mein hu` → Delhi). Note the
   first-measurement evidence that motivated the current exclusion:
   `Pune se bahar nahi jaunga` LOSES Pune under naive masking. Needs its own
   before/after run on this corpus.
2. **`cnc` / `operator` in the role gazetteer** (findings 1-2) — the single
   largest coverage gap, on an ESSENTIAL topic, on the two words the shipped
   question puts in the worker's mouth. Not a pure gazetteer edit: `operator` is
   already an operation-knowledge cue, so adding it as a role has to be
   reconciled with the `skills` auto-close, and any role id must exist in the
   ADR-0030 taxonomy rather than being minted here.
3. **`skills` auto-close** (finding 3) — a role/machine answer marking `skills`
   answered means the skills question is never asked. Marking a topic answered on
   an INFERENCE, rather than on an answer to the question, is the root cause.
4. **Spelled-out numerals and Devanagari numbers** — `char saal`, `पाँच साल`,
   `pandrah hazaar` are all gaps, and they are a large share of the `experience`
   and `salary_current` misses.
5. **`kabhi` matching `abhi`** (finding 6.2) — a word-boundary fix on one cue.
6. **Ranges and relative demands** (`18 se 20 hazar`, `abhi se 5000 zyada`,
   `30-35k`) — a CONTRACT question, not a parser one: the field holds a scalar,
   so no parser change can record what the worker said.

## Full measurement table

| asked topic | worker answer | accepted | detected keys | human |
| --- | --- | :---: | --- | --- |
| `role` | `CNC` | NO | — | accept |
| `role` | `CNC operator` | NO | `skills` | accept |
| `role` | `VMC operator` | yes | `machines`, `role`, `skills` | accept |
| `role` | `vmc chalata hu` | yes | `machines`, `role`, `skills` | accept |
| `role` | `setter hu` | yes | `role` | accept |
| `role` | `CNC programmer` | yes | `role`, `skills` | accept |
| `role` | `lathe operator` | NO | `machines`, `skills` | accept |
| `role` | `operator` | NO | `skills` | accept |
| `role` | `machine operator hu` | NO | `skills` | accept |
| `role` | `CNC machine chalata hoon` | NO | `skills` | accept |
| `role` | `turner` | yes | `role` | accept |
| `role` | `programmer hu, mastercam pe kaam karta hu` | yes | `role`, `skills` | accept |
| `role` | `main CNC operator ka kaam karta hu` | NO | `skills` | accept |
| `role` | `hmc operator` | yes | `machines`, `role`, `skills` | accept |
| `role` | `grinding operator` | yes | `machines`, `role`, `skills` | accept |
| `role` | `cnc oprator` | NO | — | accept |
| `role` | `seter ka kaam` | NO | — | accept |
| `role` | `V M C operator` | NO | `skills` | accept |
| `role` | `मैं वीएमसी ऑपरेटर हूँ` | NO | — | accept |
| `role` | `प्रोग्रामर` | NO | — | accept |
| `role` | `helper hu, machine seekh raha hu` | NO | — | accept |
| `role` | `fitter` | NO | — | accept |
| `role` | `supervisor` | NO | — | accept |
| `role` | `abhi kuch nahi kar raha, kaam dhundh raha hu` | NO | `availability` | reject |
| `machines` | `VMC` | yes | `machines`, `role` | accept |
| `machines` | `cnc` | NO | — | accept |
| `machines` | `CNC lathe` | yes | `machines` | accept |
| `machines` | `lathe` | yes | `machines` | accept |
| `machines` | `vmc aur lathe dono` | yes | `machines`, `role` | accept |
| `machines` | `turning machine` | yes | `machines`, `role` | accept |
| `machines` | `milling machine` | NO | — | accept |
| `machines` | `drilling machine` | NO | — | accept |
| `machines` | `grinding machine` | yes | `machines`, `role` | accept |
| `machines` | `surface grinder` | yes | `machines` | accept |
| `machines` | `cylindrical grinding` | yes | `machines`, `role` | accept |
| `machines` | `HMC` | yes | `machines`, `role` | accept |
| `machines` | `vmc machine par kaam karta hu` | yes | `machines`, `role` | accept |
| `machines` | `वीएमसी` | NO | — | accept |
| `machines` | `vmc, hmc dono chalata hu` | yes | `machines`, `role`, `skills` | accept |
| `machines` | `vmc 850` | yes | `machines`, `role` | accept |
| `machines` | `sirf lathe` | yes | `machines` | accept |
| `machines` | `vtl` | NO | — | accept |
| `machines` | `boring machine` | NO | — | accept |
| `machines` | `shaper machine` | NO | — | accept |
| `machines` | `power press` | NO | — | accept |
| `machines` | `welding machine` | NO | `role`, `skills` | accept |
| `machines` | `vmc bhi lathe bhi, jo bole` | yes | `machines`, `role` | accept |
| `machines` | `pata nahi naam kya hai` | NO | — | reject |
| `experience` | `4 saal` | yes | `experience` | accept |
| `experience` | `4 years` | yes | `experience` | accept |
| `experience` | `4 sal` | yes | `experience` | accept |
| `experience` | `char saal` | NO | — | accept |
| `experience` | `chaar saal ka experience hai` | NO | — | accept |
| `experience` | `do saal` | NO | — | accept |
| `experience` | `teen saal` | NO | — | accept |
| `experience` | `ek saal` | NO | — | accept |
| `experience` | `bees saal` | NO | — | accept |
| `experience` | `2.5 saal` | yes | `experience` | accept |
| `experience` | `2 saal 6 mahine` | yes | `experience` | accept |
| `experience` | `6 mahine` | NO | — | accept |
| `experience` | `10 saal se kaam kar raha hu` | yes | `experience` | accept |
| `experience` | `10+ years` | yes | `experience` | accept |
| `experience` | `3 yrs` | yes | `experience` | accept |
| `experience` | `3 year` | yes | `experience` | accept |
| `experience` | `15 saal` | yes | `experience` | accept |
| `experience` | `8 saal experience` | yes | `experience` | accept |
| `experience` | `1 saal se kam` | yes | `experience` | accept |
| `experience` | `5 साल` | NO | — | accept |
| `experience` | `पाँच साल का तजुर्बा` | NO | — | accept |
| `experience` | `2012 se kaam kar raha hu` | NO | — | accept |
| `experience` | `fresher hu` | NO | — | accept |
| `experience` | `naya hu, abhi start kiya` | NO | — | accept |
| `experience` | `yaad nahi` | NO | — | reject |
| `skills` | `setting aata hai` | yes | `skills` | accept |
| `skills` | `tool offset kar leta hu` | yes | `skills` | accept |
| `skills` | `program edit karta hu` | yes | `skills` | accept |
| `skills` | `drawing padh leta hu` | yes | `skills` | accept |
| `skills` | `gd&t aata hai` | yes | `skills` | accept |
| `skills` | `offset set karta hu` | yes | `skills` | accept |
| `skills` | `machine setup karta hu` | yes | `skills` | accept |
| `skills` | `fixture lagana aata hai` | yes | `skills` | accept |
| `skills` | `g code likh leta hu` | yes | `skills` | accept |
| `skills` | `mastercam pe programming` | yes | `skills` | accept |
| `skills` | `sab aata hai` | NO | — | accept |
| `skills` | `sab kaam aata hai setting bhi` | yes | `skills` | accept |
| `skills` | `sirf operation` | NO | — | accept |
| `skills` | `setting nahi aati, sirf chalata hu` | yes | `skills` | accept |
| `skills` | `vernier aur micrometer use karta hu` | NO | — | accept |
| `skills` | `quality checking karta hu` | NO | — | accept |
| `skills` | `sirf loading unloading` | NO | — | accept |
| `skills` | `deburring aur finishing` | NO | — | accept |
| `skills` | `cmm operate karta hu` | yes | `skills` | accept |
| `skills` | `job setting aur tool change` | yes | `skills` | accept |
| `skills` | `बेसिक सेटिंग आती है` | NO | — | accept |
| `skills` | `ड्रॉइंग पढ़ लेता हूँ` | NO | — | accept |
| `skills` | `counter boring aur tapping` | NO | — | accept |
| `skills` | `kuch nahi aata, seekh raha hu` | NO | — | reject |
| `current_location` | `Pune` | yes | `current_location` | accept |
| `current_location` | `pune mein hu` | yes | `current_location` | accept |
| `current_location` | `Delhi` | yes | `current_location` | accept |
| `current_location` | `dilli` | yes | `current_location` | accept |
| `current_location` | `Bombay` | yes | `current_location` | accept |
| `current_location` | `Rajkot` | yes | `current_location` | accept |
| `current_location` | `Faridabad` | yes | `current_location` | accept |
| `current_location` | `gurgaon` | yes | `current_location` | accept |
| `current_location` | `Noida sector 63` | yes | `current_location` | accept |
| `current_location` | `Ahmedabad ke paas` | yes | `current_location` | accept |
| `current_location` | `Hosur` | yes | `current_location` | accept |
| `current_location` | `Coimbatore` | yes | `current_location` | accept |
| `current_location` | `Aurangabad` | yes | `current_location` | accept |
| `current_location` | `Ludhiana` | yes | `current_location` | accept |
| `current_location` | `Peenya, Bangalore` | yes | `current_location`, `preferred_locations` | accept |
| `current_location` | `abhi Pune mein rehta hu` | yes | `current_location` | accept |
| `current_location` | `Chakan` | NO | — | accept |
| `current_location` | `Ranjangaon` | NO | — | accept |
| `current_location` | `Bhiwadi` | NO | — | accept |
| `current_location` | `Jamshedpur` | NO | — | accept |
| `current_location` | `Kolhapur` | NO | — | accept |
| `current_location` | `Bihar` | NO | — | accept |
| `current_location` | `पुणे` | NO | — | accept |
| `current_location` | `ghar pe hu, gaon mein` | NO | — | reject |
| `preferred_locations` | `Pune` | yes | `preferred_locations` | accept |
| `preferred_locations` | `kahin bhi chalega` | yes | `preferred_locations` | accept |
| `preferred_locations` | `Delhi ya Noida` | yes | `preferred_locations` | accept |
| `preferred_locations` | `sirf Pune` | yes | `preferred_locations` | accept |
| `preferred_locations` | `Maharashtra mein kahin bhi` | yes | `preferred_locations` | accept |
| `preferred_locations` | `bahar ja sakta hu` | yes | `preferred_locations` | accept |
| `preferred_locations` | `anywhere in India` | yes | `preferred_locations` | accept |
| `preferred_locations` | `relocate kar sakta hu` | yes | `preferred_locations` | accept |
| `preferred_locations` | `Chennai Bangalore dono chalega` | yes | `preferred_locations` | accept |
| `preferred_locations` | `abhi Pune mein hu, Delhi bhi chalega` | yes | `current_location`, `preferred_locations` | accept |
| `preferred_locations` | `Rajkot Ahmedabad` | yes | `preferred_locations` | accept |
| `preferred_locations` | `Mumbai` | yes | `preferred_locations` | accept |
| `preferred_locations` | `Hyderabad` | yes | `preferred_locations` | accept |
| `preferred_locations` | `Pune, Chakan, Ranjangaon` | yes | `preferred_locations` | accept |
| `preferred_locations` | `apne sheher mein hi` | NO | — | accept |
| `preferred_locations` | `ghar ke paas hi chahiye` | NO | — | accept |
| `preferred_locations` | `Gujarat mein` | NO | — | accept |
| `preferred_locations` | `South India` | NO | — | accept |
| `preferred_locations` | `NCR` | NO | — | accept |
| `preferred_locations` | `kahi bhi` | yes | `preferred_locations` | accept |
| `preferred_locations` | `koi bhi jagah` | yes | `preferred_locations` | accept |
| `preferred_locations` | `जहाँ भी काम मिले` | NO | — | accept |
| `preferred_locations` | `Pune se bahar nahi jaunga` | yes | `preferred_locations` | accept |
| `preferred_locations` | `abhi soch nahi paya` | NO | — | reject |
| `controllers` | `Fanuc` | yes | `controllers` | accept |
| `controllers` | `fanuc aur siemens` | yes | `controllers` | accept |
| `controllers` | `Siemens` | yes | `controllers` | accept |
| `controllers` | `Mitsubishi` | yes | `controllers` | accept |
| `controllers` | `Haas` | yes | `controllers` | accept |
| `controllers` | `Heidenhain` | yes | `controllers` | accept |
| `controllers` | `fanuc oi mate` | yes | `controllers` | accept |
| `controllers` | `fanuc series 0i` | yes | `controllers` | accept |
| `controllers` | `FANUC` | yes | `controllers` | accept |
| `controllers` | `siemens 828d` | yes | `controllers` | accept |
| `controllers` | `Mitsubishi M70` | yes | `controllers` | accept |
| `controllers` | `fanuc, mitsubishi dono` | yes | `controllers` | accept |
| `controllers` | `fanuc system hai machine mein` | yes | `controllers` | accept |
| `controllers` | `fanuk` | NO | — | accept |
| `controllers` | `phanuc` | NO | — | accept |
| `controllers` | `फैनुक` | NO | — | accept |
| `controllers` | `sinumerik` | NO | — | accept |
| `controllers` | `Mazatrol` | NO | — | accept |
| `controllers` | `Syntec` | NO | — | accept |
| `controllers` | `GSK` | NO | — | accept |
| `controllers` | `Hurco` | NO | — | accept |
| `controllers` | `Delta controller` | NO | — | accept |
| `controllers` | `controller ka naam nahi pata` | NO | — | reject |
| `controllers` | `pata nahi konsa hai` | NO | — | reject |
| `salary_current` | `22000` | yes | `salary_current` | accept |
| `salary_current` | `22k` | yes | `salary_current` | accept |
| `salary_current` | `22 hazar` | yes | `salary_current` | accept |
| `salary_current` | `15 hazaar` | NO | — | accept |
| `salary_current` | `18000 rupaye` | yes | `salary_current` | accept |
| `salary_current` | `₹20,000` | yes | `salary_current` | accept |
| `salary_current` | `20 thousand` | yes | `salary_current` | accept |
| `salary_current` | `monthly 25000` | yes | `salary_current` | accept |
| `salary_current` | `25000 per month` | yes | `salary_current` | accept |
| `salary_current` | `abhi 19500 milta hai` | yes | `salary_current` | accept |
| `salary_current` | `12 hazar` | yes | `salary_current` | accept |
| `salary_current` | `salary 30k hai` | yes | `salary_current` | accept |
| `salary_current` | `haath mein 21000 aata hai` | yes | `salary_current` | accept |
| `salary_current` | `8500` | yes | `salary_current` | accept |
| `salary_current` | `15,000` | yes | `salary_current` | accept |
| `salary_current` | `18 se 20 hazar` | yes | `salary_current` | accept |
| `salary_current` | `20000 + PF` | yes | `salary_current` | accept |
| `salary_current` | `13 thousand` | yes | `salary_current` | accept |
| `salary_current` | `sallery 17000` | yes | `salary_current` | accept |
| `salary_current` | `1.5 lakh saal ka` | yes | `salary_current` | accept |
| `salary_current` | `daily 700 rupaye` | NO | — | accept |
| `salary_current` | `pandrah hazaar` | NO | — | accept |
| `salary_current` | `पंद्रह हज़ार` | NO | — | accept |
| `salary_current` | `batana nahi chahta` | NO | — | reject |
| `salary_expected` | `25000` | yes | `salary_expected` | accept |
| `salary_expected` | `25k chahiye` | yes | `salary_expected` | accept |
| `salary_expected` | `30 hazar expect karta hu` | yes | `salary_expected` | accept |
| `salary_expected` | `35000 chahiye` | yes | `salary_expected` | accept |
| `salary_expected` | `kam se kam 25000` | yes | `salary_expected` | accept |
| `salary_expected` | `30-35k` | yes | `salary_expected` | accept |
| `salary_expected` | `40000` | yes | `salary_expected` | accept |
| `salary_expected` | `1 lakh` | yes | `salary_expected` | accept |
| `salary_expected` | `22k se upar` | yes | `salary_expected` | accept |
| `salary_expected` | `28000 minimum` | yes | `salary_expected` | accept |
| `salary_expected` | `35 thousand` | yes | `salary_expected` | accept |
| `salary_expected` | `30k plus rehna` | yes | `salary_expected` | accept |
| `salary_expected` | `24000 chahiye ghar ke liye` | yes | `salary_expected` | accept |
| `salary_expected` | `25 hazaar chahiye` | NO | — | accept |
| `salary_expected` | `abhi se 5000 zyada` | yes | `salary_expected` | accept |
| `salary_expected` | `double chahiye` | NO | — | accept |
| `salary_expected` | `jo aap theek samjhe` | NO | — | accept |
| `salary_expected` | `aapke hisab se` | NO | — | accept |
| `salary_expected` | `company jo de` | NO | — | accept |
| `salary_expected` | `jitna aap de sako` | NO | — | accept |
| `salary_expected` | `salary aapki marzi` | NO | — | accept |
| `salary_expected` | `negotiable` | NO | — | accept |
| `salary_expected` | `जो भी मिले` | NO | — | accept |
| `salary_expected` | `koi fix nahi` | NO | — | reject |
| `availability` | `abhi turant` | yes | `availability` | accept |
| `availability` | `immediate` | yes | `availability` | accept |
| `availability` | `15 din` | yes | `availability` | accept |
| `availability` | `30 din` | yes | `availability` | accept |
| `availability` | `7 din` | yes | `availability` | accept |
| `availability` | `20 din lagenge` | yes | `availability` | accept |
| `availability` | `10 days` | yes | `availability` | accept |
| `availability` | `ek mahina` | yes | `availability` | accept |
| `availability` | `do mahine baad` | yes | `availability` | accept |
| `availability` | `notice period hai` | yes | `availability` | accept |
| `availability` | `notice de diya hai` | yes | `availability` | accept |
| `availability` | `next month` | yes | `availability` | accept |
| `availability` | `1 week` | yes | `availability` | accept |
| `availability` | `2 hafte` | yes | `availability` | accept |
| `availability` | `ek hafte mein` | yes | `availability` | accept |
| `availability` | `abhi free hu` | yes | `availability` | accept |
| `availability` | `abhi available hu` | yes | `availability` | accept |
| `availability` | `turant join kar sakta hu` | yes | `availability` | accept |
| `availability` | `aaj se ready hu` | yes | `availability` | accept |
| `availability` | `kal se` | yes | `availability` | accept |
| `availability` | `abhi job kar raha hu, 1 mahina lagega` | yes | `availability` | accept |
| `availability` | `जल्दी जॉइन कर सकता हूँ` | NO | — | accept |
| `availability` | `पंद्रह दिन` | NO | — | accept |
| `availability` | `jab bolo tab` | yes | `availability` | accept |
| `education` | `ITI kiya hai` | yes | `education` | accept |
| `education` | `iti` | yes | `education` | accept |
| `education` | `ITI fitter` | yes | `education` | accept |
| `education` | `ITI turner trade` | yes | `education`, `role` | accept |
| `education` | `ITI electrician` | yes | `education` | accept |
| `education` | `diploma mechanical` | yes | `education` | accept |
| `education` | `polytechnic diploma` | yes | `education` | accept |
| `education` | `B.Tech mechanical` | yes | `education` | accept |
| `education` | `B.E. mechanical` | NO | — | accept |
| `education` | `NSDC certificate` | yes | `education` | accept |
| `education` | `RVM CAD course kiya` | yes | `education` | accept |
| `education` | `ITI + 3 saal apprenticeship` | yes | `education`, `experience` | accept |
| `education` | `10th pass` | NO | — | accept |
| `education` | `12th pass` | NO | — | accept |
| `education` | `8th pass` | NO | — | accept |
| `education` | `graduation kiya hai` | NO | — | accept |
| `education` | `BA pass` | NO | — | accept |
| `education` | `apprentice kiya tha` | NO | — | accept |
| `education` | `CNC ka course kiya private institute se` | NO | — | accept |
| `education` | `आईटीआई किया है` | NO | — | accept |
| `education` | `डिप्लोमा किया है` | NO | — | accept |
| `education` | `iti nahi kiya, kaam se hi seekha` | yes | `education` | reject |
| `education` | `diploma nahi hai` | yes | `education` | reject |
| `education` | `school chhod diya tha` | NO | — | reject |

