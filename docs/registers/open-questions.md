# Open Questions

Unknowns that block or shape decisions. When answered, move the row to
**Resolved** with the answer + where it was recorded (ADR / team-decision).

Seeded 2026-06-09.

| ID | Question | Why it matters | Status |
| -- | -------- | -------------- | ------ |
| Q1 | Which **real OTP provider** (MSG91 / Gupshup / Twilio / Firebase)? Cost, deliverability in tier-2/3 India, latency? | Blocks TD2 and real onboarding | Open |
| Q2 | **Unlock pricing model** — per-unlock credits vs employer subscription tiers vs hybrid? Price points? | Drives Phase-2 payments design + PRD + cost model | **Resolved (2026-06-15, [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §Sign-off resolutions)** — **payer-side credit packs** (NOT subscription, NOT hybrid): **₹1000 → 10 profile-unlock credits**, **₹2000 → 25 credits**. Granularity is **per-candidate-profile** (one credit unlocks one candidate's profile + resume + routed contact), with a **14-day** pack/credit validity window. **Workers/applicants are never charged and have no apply cap**; the **payer** (an **employer OR an agent**, whoever performs the unlock) is charged. Packs are config-driven; the unlock primitive is "debit 1 credit → grant 1 unlock" so a subscription/hybrid model later is just a crediting policy on top. Real Razorpay purchase is a separate human-gated stream ([TD34](./tech-debt-register.md)); alpha runs on the mock credit ledger |
| Q3 | Default **LLM provider/model** for extraction & canonicalization? Cost/latency/quality trade-off? | Drives R6 cost projection and staging enablement | **Resolved (2026-06-15, [ADR-0008](../decisions/0008-litellm-to-direct-providers.md))** — direct providers: Gemini (primary, capable=`gemini-2.5-flash`, cheap=`gemini-2.5-flash-lite`) → Claude Haiku 4.5 fallback → mock. **Cost/quality CONFIRMED in staging (2026-06-15, Claude Haiku primary, behind the TD27 cap): 95% per-field accuracy (151/159), 56/56 real calls clean (0% error), ₹0.267/call (≈₹0.27–0.5/worker), 0 over-masking.** The written, numeric **prod-flip threshold** is in [ADR-0008](../decisions/0008-litellm-to-direct-providers.md). Remaining before a prod flip (not blocking this resolution): latency not yet measured + ≥2 more staging runs + shared-store ledger + secrets manager. **Flip blocker (2026-06-17 audit, [GO/NO-GO Finding 4](../ai/real-llm-flip-go-no-go.md)) — CONFIG PINNED:** the 95% run was on **Claude Haiku primary** (cost/latency on flash-lite) — neither the prod model. The prod extraction model is now **PINNED to `gemini-2.5-flash`** (`default_capable_model` in `app/config.py` aligned to the runbook + ADR-0008 capable tier), so validation-model must equal flip-model. **Still owed before the flip (human-gated, real paid calls): a clean 56/56 gold-set run on `gemini-2.5-flash` (role ≥90% + per-field ≥90%, zero mock-fallback) + a p95 latency number on that model** — run on a funded staging key per [enable-real-llm-extraction.md](../ai/enable-real-llm-extraction.md) §"Re-validation on the pinned model". If <90% on the pinned model, STOP |
| Q4 | **Sarvam STT** contract, pricing, latency, language coverage (Hindi + regional)? | Blocks TD6 voice profiling | Open |
| Q5 | Concrete **RLS model** — how do we map worker auth identity → `workers` row for per-worker isolation? | Blocks R1/TD4; **now also blocks the payer portal** | **Open — escalated.** [ADR-0019](../decisions/0019-self-serve-payer-portal.md) (self-serve payer portal) makes RLS a **hard launch-gate requirement**, not a backlog item: external untrusted payers need DB-enforced **two-axis** isolation (payer↔payer tenancy + payer↔worker PII). ADR-0019 Decision C adds the **payer** identity→row mapping (`current_payer_id()` via least-privilege role or request-scoped `SET LOCAL`) to be designed **with** this worker mapping; the app-layer tenant chokepoint is the interim (closed-beta) control, DB RLS is the open-GA gate. Resolve jointly. |
| Q6 | **DPDP data-residency** requirement — must all PII stay in an India region? Affects Supabase region + any provider | Launch gate; affects infra | Open |
| Q7 | **Scale targets** — workers onboarded and employers active at 6 / 12 months? | Needed for cost projection + capacity planning | Open |
| Q8 | **Embeddings/model-training tables** were frozen into the schema — what is the intended first use (semantic search? matching?) and when? | Shapes Phase-2 AI roadmap | **Resolved (2026-06-17, [ADR-0017](../decisions/0017-learn-layer-offline-rank-calibration.md))** — premise is **stale**: there is **no `model_training` table and no standalone `embeddings` table** in the current schema (ADR-0014 foundation). The only embedding is **`worker_profiles.embedding`** (768-dim Vertex, HNSW cosine), which per **ADR-0006** serves **AI profiling / semantic similarity — NOT Reach ranking**. The **LEARN layer's first use** is **offline learning-to-rank over the PII-free `events` stream to calibrate the deterministic RANK dials** — needing **neither** embeddings **nor** a frozen ML table: training data = the event spine (+ point-in-time signal snapshot), artifact = a versioned **`WeightProfile` config file** (offline-first, DB-light). A model/artifact registry (`reach_models`) would be an **additive** Phase-2 migration if ever needed, **not** the imagined frozen table. (ADR-0017 is **PROPOSED — pending sign-off**; the Q8 *resolution* — embeddings≠ranking, no frozen ML tables required — holds regardless.) |
| Q9 | **Reusable consent gate** ([ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) OQ-1) — the API had no shared "require active consent before this action" primitive; chat/profiling enforced the DPDP gate at their own boundaries. The alpha apply/skip routes need it too | Net-new shared surface; the alpha swipe-to-apply routes must sit behind the consent gate (invariant 6) | **Resolved (2026-06-15, [ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md))** — a reusable `ConsentGuard` (`apps/api/src/auth/consent.guard.ts`) was built and applied to all three worker routes (feed/apply/skip); passed two security reviews |
| Q10 | **`trade_key` taxonomy linkage** ([ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) OQ-2) — `@badabhai/db` cannot import the 15-key alpha-trade list from `apps/api` or `@badabhai/event-schema` (dependency direction), so `jobs.trade_key` mirrors `TradeKey` (+ `SkipReason`/`SourceSurface`) as local unions with "keep in sync" comments. Source of truth = `REQUIRED_TRADE_KEYS` in `apps/api` | Two copies of the enum can drift; feed/profile trades must align | Open — follow-up: extract a shared `taxonomy/enums` package so `@badabhai/db` and `apps/api` consume one source (tracked as tech-debt, [TD31](./tech-debt-register.md)) |
| Q11 | **RLS for `jobs` + `applications`** ([ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) OQ-3) — both new tables are accessed via the backend service role today (ADR-0004), like the rest of Phase-1; `applications.worker_id` references `workers` | Must be covered when RLS is finalised so the new tables join the locked spine (TD20) | Open — add `jobs` + `applications` to the [rls-plan](../../infra/supabase/rls-plan.md) when RLS lands (cross-link [TD4/TD20](./tech-debt-register.md), [R1/R15](./risks-register.md)). **Update (2026-06-15, [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md)):** the four new unlock tables (`unlocks`/`payer_credits`/`credit_ledger`/`unlock_routing`) join the same backlog — add all six when RLS lands (cross-link [R20](./risks-register.md)) |
| Q12 | **Worker-protection cap numbers for Contact Unlock** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D4/OQ-F) — the per-worker reveal/payer caps (recommended starting points: ≤5 reveals/worker/day, ≤10 distinct payers/worker/week, ≤3 routed attempts/unlock) are **config-driven** and set at build, not yet finalized | The cap mechanism (a single fail-closed `UnlockGuardService` chokepoint, enforced atomically before payment) is locked architecture; only the numbers are tunable. They are a trust-and-safety call and can change without a migration | Open (small) — the chokepoint + config seam shipped in Stream A; confirm/tune the starting numbers as a trust-and-safety decision. Cross-link [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D4 |
| Q13 | **Ratified industrial `ADJACENT_ROLES` map for PACE** ([ADR-0021](../decisions/0021-pace-supply-widening-and-ops-alert.md), Q-PACE-ADJ) — the PACE adjacent-trade widen leg needs a product-ratified related-trade map for the launch CNC/VMC roles; none exists (taxonomy has none; hospitality declined one 2026-06-17; industrial is draft). The reach engine is adjacency-ready (`secondaryRoleIds` @0.6) — only the DATA is missing | The adjacent-trade leg is built but **gated** (`PACE_ADJACENCY_ENABLED=false`); AREA-widen + OPS-ALERT ship without it. Wiring a draft/guessed map would silently mis-target supply | Open — **escalated to Product/CEO** ([pace-adjacency-ratification-escalation.md](./pace-adjacency-ratification-escalation.md)): ratify a small reviewed adjacency table (→ wire + flip the gate + below-on-trade test) **or** defer explicitly (mirror hospitality). Cross-link [TD45](./tech-debt-register.md) |

> Keep this list short and live. A question that's been "Open" for months either
> needs an owner and a decision, or it isn't really blocking — say which.
