# Decisions Log

The chronological index of every decision that shaped BadaBhai. Heavyweight /
architectural decisions get a full **ADR** in [`docs/decisions/`](../decisions/);
lightweight calls live in [team-decisions.md](./team-decisions.md). This file
indexes both so there is one timeline.

| Date | Decision | Type | Where |
| ---- | -------- | ---- | ----- |
| 2026-06-08 | MVP infra & AI: Supabase Postgres, API-first AI via LiteLLM, no self-hosted LLM, mandatory fail-closed pseudonymization, Flutter, worker-profiling-first | ADR | [0001-mvp-infra-decision.md](../decisions/0001-mvp-infra-decision.md) |
| 2026-06-09 | Revenue model = **employer/agency pays to unlock candidates**; workers free | Team | [team-decisions.md](./team-decisions.md) |
| 2026-06-09 | Team size assumption = **2–5**; quality gates calibrated to automation + one human reviewer | Team | [team-decisions.md](./team-decisions.md) |
| 2026-06-09 | Immediate priority = **close Phase-1 "next" items** (BullMQ jobs, real OTP, RLS, STT, real LLM in staging) | Team | [team-decisions.md](./team-decisions.md) |
| 2026-06-09 | Adopted the **engineering-org layer** (agents, skills, workflow, gates, registers) | Team | [team-decisions.md](./team-decisions.md) |
| 2026-06-09 | **Async profile extraction (BullMQ)** + **generic events-only action recorder** (`action.recorded`); `/profile/extract` → `202` + poll | ADR | [0002-async-extraction-and-action-recording.md](../decisions/0002-async-extraction-and-action-recording.md) |
| 2026-06-12 | **Reach foundation — deterministic RANK core + behavioural event contracts** ratified (Proposed→Accepted). Implemented weights are authoritative; the locked "Skills 15" + Vertex skills-similarity is deferred to Phase 2/LEARN | ADR | [0006-reach-foundation-rank-core.md](../decisions/0006-reach-foundation-rank-core.md) |
| 2026-06-15 | **LiteLLM → direct Gemini/Claude provider calls** — ratifies the shipped direct-provider stack (Gemini primary + Claude Haiku fallback behind the `LlmAdapter`/`AIRouter` seam); supersedes ADR-0001 §3. Closes TD28 (env unify on `GEMINI_FLASH_API_KEY`); names the TD27 spend-cap hook | ADR | [0008-litellm-to-direct-providers.md](../decisions/0008-litellm-to-direct-providers.md) |
| 2026-06-15 | **Alpha swipe-to-apply on seeded jobs** — scoped early activation of the ADR-0006 behavioural events (`feed.shown`/`application.submitted`/`application.skipped`) via a seeded `jobs` + `applications` producer (additive, PII-free, no payload version bump). Three consent-gated worker routes (feed/apply/skip) + two PII-free ops reads; introduces a reusable `ConsentGuard` (resolves the ADR's OQ-1). NO ranking (score=0, rank=seed order), employer console, unlock, payments, or LLM — all Phase-2 surfaces stay OUT. Crosses the Phase-1/2 line narrowly + deliberately, human-gated | ADR | [0009-alpha-swipe-to-apply-seeded-jobs.md](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) |
| 2026-06-15 | **Contact Unlock + Reveal — routed-disclosure monetization spine** (Phase-0 design, **Accepted 2026-06-15**). Fail-closed disclosure ordering (consent→caps→payment→grant→routed-reveal); raw phone read only at the final routed-reveal, never in events/`ai_jobs`/`audit_logs`/logs. **Pricing (resolves Q2): payer-side credit packs — ₹1000/10 profiles, ₹2000/25 profiles, per-profile, 14-day window; workers never charged; payer = employer or agent.** Payments = **real gateway (human-gated escalation in progress)**; `PAYMENTS_ENABLE_REAL=false` mock is the code default until provider/keys/staging confirmed. In-app relay (no telephony provider) + raw-never in alpha; separate `employer_sharing` DPDP consent; config-driven per-worker caps at a single chokepoint. PII-disclosure threat model written; bb-security-review = CONCERNS with F-1/F-2 build-blockers folded in (build authorized once pinned) | ADR | [0010-contact-unlock-and-reveal.md](../decisions/0010-contact-unlock-and-reveal.md) |
| 2026-06-15 | **Ops-created, vacancy-banded, stored-only Job Postings** (alpha-gate) — additive `job_posting` event domain + `job_postings` table; banded vacancy enum (`1`/`2-5`/`6-10`/`11-25`/`25+`); opaque ops `created_by`; NON-PII free text stored-only (never in events); `draft→open→closed` lifecycle (closed terminal). A distinct entity from the merged ADR-0009 swipe `jobs`/`applications` (no bridge). Amended per the principal-engineer review (D1/D2/D3). *(Renumbered ADR-0010→0012 at merge: `main` independently assigned 0010 to Contact Unlock.)* | ADR | [0012-ops-job-postings-banded-stored-only.md](../decisions/0012-ops-job-postings-banded-stored-only.md) |
| 2026-06-15 | **Reach feed serving** (alpha-gate) — strictly-additive `apps/api/src/reach` serving layer over the unchanged `@badabhai/reach-engine`: View A payer applicant list (`rankWorkersForJob`) + View B worker job feed (`scoreWorkerForJob`), faceless, read-only ops. `JobSource` port + `StubJobSource` (swaps to `job_postings`). Reuses `feed.shown` (emitted **UNKEYED** per the review's D7 flip). Coexists with the ADR-0009 swipe-to-apply producer (both emit `feed.shown`; different surfaces). Amended per the principal-engineer review (D4-D8) | ADR | [0011-reach-feed-serving.md](../decisions/0011-reach-feed-serving.md) |
| 2026-06-16 | **Employer/agent monetization + config-driven Pricing Engine** (Phase-0, **Accepted 2026-06-16**). Hybrid pricing engine (`@badabhai/pricing`: typed Zod catalog + deterministic fail-closed resolve; ops-editable values + discounts/offers/coupons; absorbs credit-packs.ts). Paid job-posting plans + booster on `job_postings`; **resume download is FREE** (maintainer change) but still a PII disclosure riding the ADR-0010 consent+caps spine; mock payments default, Razorpay env documented-but-disabled. Threat-model addendum is a hard pre-build gate for resume disclosure | ADR | [0013-monetization-and-config-driven-pricing-engine.md](../decisions/0013-monetization-and-config-driven-pricing-engine.md) |
| 2026-06-17 | **Phase-1 schema foundation — declared STABLE, additive-only going forward** (**ACCEPTED — CEO-signed 2026-06-17**). Creates the previously-undocumented "item-10" gate as a concrete checklist + CEO sign-off block. **Not a hard freeze** — Phase-2 additive tables (monetization/reach) continue; the commitment is a change policy (additive + versioned + ADR for any breaking change, the operational form of invariant 8) over the current **25 tables** (migrations through `0016`). Items 1–9 closed; item-10 = the CEO declaration. Reconciles the stale schema doc (10→25 tables) | ADR | [0014-phase-1-schema-foundation-stable.md](../decisions/0014-phase-1-schema-foundation-stable.md) |
| 2026-06-17 | **Per-payer hiring capacity — concurrent-active-vacancy allowance** (alpha-gate, **Phase-0 signed 2026-06-17**). Extends [ADR-0013](../decisions/0013-monetization-and-config-driven-pricing-engine.md) Decision B additively: a new `payer_capacity` table (allowance + tier code + window; the active count is **DERIVED**, no side counter) + a widened `posting_plans.status` (`paused`) + a `hiring_capacity` pricing product (`cap_5` ₹5000/5/30d, `cap_15` ₹12000/15/30d) + three v1 events (`capacity.purchased`, `posting_plan.paused/.resumed`) + `POST /payers/:payerId/capacity` (migration 0019). One atomic chokepoint (per-payer `pg_advisory_xact_lock` + deferred post-commit emit, ADR-0010 F-2) counts/decides on plan buy and auto-resumes oldest-paid paused plans on a capacity buy. **Enforcement is INERT by default (D5):** `CAPACITY_ENFORCEMENT_ENABLED` (default false) gates whether `buyPlan` actually pauses an over-cap plan — default = shadow (compute the would-pause, leave the plan active, log PII-free + a `wouldPause` flag, NO spine event); a future one-event `capacity.evaluated` upgrade path is noted but not built. Mock payments only; `payer_id` is **advisory until `PayerAuthGuard`/LC-1** (`InternalServiceGuard`). Deferrals → TD43 | ADR | [0016-payer-hiring-capacity.md](../decisions/0016-payer-hiring-capacity.md) |

## How to add a decision

1. Decide whether it's **ADR-worthy** (architectural, hard to reverse, cross-cutting)
   or **lightweight** (priority, scope, vendor lean, process).
2. ADR → copy the format of [ADR-0001](../decisions/0001-mvp-infra-decision.md),
   number it sequentially (`000N-<slug>.md`), set Status.
3. Lightweight → add a dated row to [team-decisions.md](./team-decisions.md).
4. Either way, **add a row here** so the timeline stays complete.

## ADR index

- [0001 — MVP Infrastructure & AI Decisions](../decisions/0001-mvp-infra-decision.md) — *Accepted*
- [0002 — Async Profile Extraction (BullMQ) + Generic Action Recording](../decisions/0002-async-extraction-and-action-recording.md) — *Accepted*
- [0006 — Reach foundation: deterministic RANK core + behavioural event contracts](../decisions/0006-reach-foundation-rank-core.md) — *Accepted (ratified 2026-06-12)*
- [0008 — LiteLLM → direct Gemini/Claude provider calls](../decisions/0008-litellm-to-direct-providers.md) — *Accepted (supersedes ADR-0001 §3)*
- [0009 — Alpha swipe-to-apply on seeded jobs](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) — *Accepted (2026-06-15; scoped alpha producer for the ADR-0006 events)*
- [0010 — Contact Unlock + Reveal (routed-disclosure monetization spine)](../decisions/0010-contact-unlock-and-reveal.md) — *Accepted (2026-06-15; payer credit-pack pricing, real-gateway escalation in progress, build authorized after F-1/F-2). **Stream A built + verified 2026-06-17** (mock credits + in-app relay; security re-review PASS; launch gates LC-1…LC-7 open — TD33/TD34/TD35/TD41)*
- [0012 — Ops-created, vacancy-banded, stored-only Job Postings](../decisions/0012-ops-job-postings-banded-stored-only.md) — *Accepted (alpha-gate, 2026-06-15)*
- [0011 — Reach feed serving (applicant list + worker feed over the RANK core)](../decisions/0011-reach-feed-serving.md) — *Accepted (alpha-gate, 2026-06-15)*
- [0013 — Employer/agent monetization + config-driven Pricing Engine](../decisions/0013-monetization-and-config-driven-pricing-engine.md) — *Accepted (2026-06-16; resume download free; Razorpay disabled; threat-model addendum gated)*
- [0014 — Phase-1 schema foundation: declared stable, additive-only](../decisions/0014-phase-1-schema-foundation-stable.md) — *ACCEPTED (CEO-signed 2026-06-17)*
- [0015 — Reach feed serving on the real `jobs` entity](../decisions/0015-reach-feed-on-real-jobs.md) — *Accepted (2026-06-17; executes the ADR-0011 §4 JobSource swap; additive demand-signal columns; RANK core unchanged; pays down TD36c)*
- [0016 — Per-payer hiring capacity (concurrent-active-vacancy allowance)](../decisions/0016-payer-hiring-capacity.md) — *Accepted (alpha-gate, 2026-06-17; extends ADR-0013 B; additive `payer_capacity` + widened `posting_plans.status` + `hiring_capacity` product + 3 v1 events; atomic per-payer chokepoint; **enforcement INERT by default — `CAPACITY_ENFORCEMENT_ENABLED` default false, D5**; mock payments; advisory `payer_id` until PayerAuthGuard/LC-1 — TD43)*
- [0017 — LEARN layer: offline learning-to-rank that calibrates the deterministic RANK core](../decisions/0017-learn-layer-offline-rank-calibration.md) — ***ACCEPTED for the OFFLINE build (2026-06-17) — LIVE promotion a SEPARATE human gate** (Phase-2 moat, NOT alpha-gate; classical LTR over the PII-free `events` stream → a versioned bounded `WeightProfile` consumed through ADR-0006's existing dial seam; model ASSISTS/never decides). **Built: `@badabhai/reach-learn`** (feature pipeline + baseline model + eval + shadow harness, 24 tests green; [eval results](../reach/learn-layer-eval-results.md)) — RANK core **unchanged**, off by default; "widen-never-narrow" enforced as a measured gating metric (cold-cohort exposure widens, held-out gate rejects regressions); **resolves Q8***
- [0018 — In-house model-training track: consent-scoped, de-identified voice/transcript corpus → fine-tune](../decisions/0018-model-training-corpus-and-finetune.md) — ***ACCEPTED — OFFLINE privacy core BUILT (CEO sign-off 2026-06-18)** (Phase-2 moat, NOT alpha-gate; OFFLINE only). **Built** in `apps/ai-service/app/corpus/`: fail-closed `CorpusConsentGate` (active-`model_training` only, revocation propagates), corpus-grade de-identifier (exclude-on-doubt, residual scan over `pseudonymize.py`), PII-free corpus assembly (provenance + de-id text only), small-sample **dry-run** fine-tune + **canary PII-leakage** eval — **§D2 build-blocker proof tests green** (consent fail-closed, sentinel-PII, residual-scan, schema; 22 tests). **STILL HUMAN-GATED (separate, NOT cleared):** full training compute/GPU spend (`run_full_training()` refuses), additive `training_corpus_items`/`_versions` DB tables (§D4 database-architect stream), **real NER hardening (TD3)** — `profile="ner"` raises so a REAL corpus is blocked until pinned + security-signed-off, and any live serving. MANDATORY security re-review on realized code owed. First concrete use of the `model_training` consent (refines Q8)*
- [0019 — Self-serve payer portal: ops-run → EXTERNAL self-serve](../decisions/0019-self-serve-payer-portal.md) — ***ACCEPTED (maintainer sign-off 2026-06-18) — Phase-0 signed, PHASE 1 AUTHORIZED** (Phase-2, NOT alpha-gate; A–E + the invariant-#2 payer-PII extension B-R2 accepted). Phase 1 is **mock + staging-only** with a `bb-security-review` PASS on realized code required before merge; **real Razorpay keys/spend (D) and open external GA (Phase 4) remain separate HUMAN gates**. Five decisions: **(A) surface** = new external app `apps/payer-web`, NOT a section of the internal `apps/web` ops console; **(B) identity/auth** = net-new `payers` account + `PayerAuthGuard` (builds the deferred LC-1/TD33), three distinct principals (worker/payer/ops), **payer/employer B2B PII = a NEW class** → explicit invariant-#2 extension (ADR-0004-protected, never in events) **[escalate B-R2]**; **(C) tenancy/RLS** = hard two-axis isolation (payer↔payer + payer↔worker), app-layer tenant chokepoint first + horizontal-authz test, **DB-enforced RLS a HARD launch gate** (resolves payer half of Q5, coordinates ADR-0004); **(D) real payments** = Razorpay hosted-checkout + signed webhook + server-side amount + idempotent capture behind `PAYMENTS_ENABLE_REAL=false`, **real keys/spend HUMAN-GATED** (TD34/LC-5); **(E) external disclosure** = re-run the disclosure threat model for an adversarial payer ([companion addendum](../security/payer-portal-external-disclosure-threat-model-addendum.md), MANDATORY security) — masking/caps/no-oracle/no-bulk hold against attacker, per-payer caps now enforceable, **account-farming** backstopped by the payer-independent per-worker cap. Phased plan: 0 ADRs → 1 identity+tenancy (mock, staging) → 2 RLS+launch hardening → 3 real payments (human-gated) → 4 open GA. Additive only; no code/app/enum built.*
- [0020 — WhatsApp invite funnel + worker re-engagement](../decisions/0020-whatsapp-invite-funnel-and-reengagement.md) — ***ACCEPTED for the MOCK build (2026-06-18) — REAL SENDS a SEPARATE human gate** (Phase-2, NOT alpha-gate; provider-and-consent-first). **(1) Provider:** WhatsApp Business API behind a `WhatsAppProvider` seam (mirrors SmsProvider) — **MockWhatsAppProvider default**, real Meta impl gated by `MESSAGING_ENABLE_REAL=false` + keys (fails closed; real send unimplemented until Phase 3); the raw phone touches the provider ONLY, never logged/evented. **(2) Consent:** NEW explicit `whatsapp_messaging` DPDP purpose (distinct from transactional `communication`); fail-closed `MessagingConsentService` — no send without it (→ `messaging.suppressed`). **(3) Attribution:** PII-free `invites` deep-links (opaque code + worker ids; RLS+REVOKE migration 0021); upstream signal for the deferred agency-referral payout. **Built (mock):** provider seam + consent gate + invite funnel + re-engagement flows + 7 PII-free v1 events (`invite.*`/`messaging.*`) + controller, wired into AppModule. Tests: consent fail-closed, **phone never in any event**, attribution/self-invite anti-abuse, mock-default (api 403 green, event-schema 64). **Real keys/spend + production WhatsApp opt-in copy = HUMAN gate (§7); `MESSAGING_ENABLE_REAL` stays false.*
- [0021 — PACE supply-widening + ops alert (deterministic "release waves")](../decisions/0021-pace-supply-widening-and-ops-alert.md) — ***ACCEPTED (PHASE-0 signed 2026-06-19) — built additively** (Phase-2 alpha-gate; implements the supply-widening + ops-alert slice of ADR-0011's deferred PACE triad). Deterministic/NO LLM: a thin-supply job widens its served good-fit pool in DELAYED BullMQ waves (**first delayed-job consumer**) — `base → AREA (raise travel band) → [GATED] adjacent trade → OPS ALERT` after the 6–24h window. Widen DECISION is a pure fn; supply = above-floor (`hot`) count via the reused RANK core (sort-never-block + floor preserved — only ever ADDS). **Inert by default** (`PACE_ENABLED=false`); **adjacent-trade leg GATED** on a ratified `ADJACENT_ROLES` map that doesn't exist (escalated — Q13/TD45). Additive: faceless `pace_states` (migration 0023, RLS-locked) + 2 PII-free v1 events (`pace.wave_widened`/`pace.ops_alert_raised`, opaque job_id + counts + stage + elapsed only; event-schema 66) + config gates; RANK core + shipped payloads unchanged. Real worker-app push/telephony OUT. Tests: decision determinism + escalation order + 6–24h timing (injected clock) + PII-free + floor (20 api + 4 event-schema).*
- [0022 — Agency Supply Portal (additive demand slice on the payer account)](../decisions/0022-agency-supply-portal.md) — ***ACCEPTED (merged #122, 2026-06-22; Phase-0 design, "Agent-Only" role model)** — classifies the agency-portal modules into BUILD-NOW (demand slice) / PARKED / LEGAL_MONEY_GATE / DEAD; the agency is an existing `payers` row with `role='agent'` (ADR-0019 account REUSED, no new principal). **BACKEND demand-slice built additively (#127, 2026-06-22):** a new VERTICAL-authz primitive `PayerRoleGuard` + `@PayerRoles('agent')` (fail-closed; `role: PayerRole|null` carried on `AuthenticatedPayer`; distinct from the `assertPayerOwns` tenant/horizontal authz) gating the new `payer/agency` module — agent-only job CRUD over `jobs.payer_id` (POST/GET/PATCH + close/pause, status stays `open|closed`), faceless invite mint (`POST /invites`, opaque code only, NO worker_id, per-payer hourly cap via PayerDisclosureRateLimit), an agency-scoped mock click stub, and an AGGREGATE-ONLY `GET /referrals/summary` (k-anon floor MIN_BUCKET=5); applicants REUSE the shipped `/payer/reach/jobs/:jobId/applicants`. New faceless `agency_invites` table (migration 0025, FORCE-RLS + REVOKE, NO KYC/money columns) + 5 new PII-free v1 events (`job.created/updated/closed`, `agency_invite.created/accepted` — no field VALUES, no code carried; event-schema registry 69→74). The consent-gated attribution seam `AgencyService.attributeWorkerToInvite` is built but INERT (no caller — TD48). Config `AGENCY_INVITE_MINT_MAX_PER_HOUR` (default 60). Dual security gate (security-engineer + security-reviewer) BOTH PASS; all 5 Appendix-C conditions confirmed + an end-to-end vertical-authz test. Mock + staging-only; KYC / payouts / real comms-payments / matching / outcome-tracking remain DEFERRED (§8). The **payer-web FRONTEND is HELD pending reconciliation** with the parallel agency frontend merged via #123/#107 (NOT in #127).*
- [Hospitality vertical (PRD)](../sprint-plans/hospitality-vertical-prd.md) — ***CONTENT DRAFTED, PENDING RVM** (not an ADR; product-scope decision; product + CEO-signed 2026-06-18). Opens a **second vertical** beyond manufacturing (net-new, NOT alpha). **Built (drafted, not live):** 9 `hosp_*` trades in `hospitality-trade-content.ts` (`HOSPITALITY_TRADE_CONTENT`) + `hospitality-interview-kit-content.ts` (`HOSPITALITY_INTERVIEW_KITS`), reusing the **exact** `TradeContent`/`InterviewKitContent` interfaces (**no new fields, no adjacency map**); presence/shape tests green (12); `TradeKey` union extended additively (mirror-and-sync, Q10/TD31 deferred); per-trade content transcribed verbatim in the [ratification packet](./hospitality-trade-content-ratification-packet.md). **Manufacturing unaffected** (separate modules; live resolver untouched). **NOT live** until per-trade **RVM PASS** (human gate).*
