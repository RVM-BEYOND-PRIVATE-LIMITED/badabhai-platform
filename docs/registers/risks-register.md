# Risks Register

Severity: **Critical** (privacy leak / auth bypass / data loss / fail-open AI) ·
**High** · **Medium** · **Low**. Status: **Open** · **Mitigated** · **Accepted** ·
**Closed**.

Seeded 2026-06-09 from ADR-0001, the architecture overview, and the Phase-1 plan.

| ID | Risk | Sev | Status | Mitigation | Owner |
| -- | ---- | --- | ------ | ---------- | ----- |
| R1 | **Supabase RLS not finalized**; API uses the service role, so any accidental client-side or mis-scoped path could read across workers | High | Open | Service role stays server-only; never shipped to web/worker apps. RLS plan in [rls-plan.md](../../infra/supabase/rls-plan.md). Finalize before any direct client→DB access. | Security + DB |
| R2 | **Pseudonymization is heuristic** (regex + gazetteers); a missed entity could leak PII to an LLM | Critical (if real calls on) | Mitigated | Over-masking bias; **fails closed** on residual digit runs/oversize/parse error; `AI_ENABLE_REAL_CALLS=false` by default; real NER planned. See [pseudonymization.md](../../docs/ai/pseudonymization.md). | AI + Security |
| R3 | **Mock providers** (OTP, STT, LLM, payment) must be replaced before any real launch; easy to forget one | High | Open | Tracked in [tech-debt](./tech-debt-register.md); launch checklist gates each. | DevOps + Backend |
| R4 | **DPDP consent flows are structural placeholders**; production legal copy/flow not done | High | Open | Consent captured structurally now; `docs/legal-later` holds the gap. Launch gate. | Product + Security |
| R5 | **No disaster-recovery runbook / tested restore** for Supabase Postgres yet | Medium | Open | Rely on Supabase managed backups in Phase 1; write + test a [DR runbook](./tech-debt-register.md) before launch. | DevOps |
| R6 | **API-first LLM cost/latency at scale unknown**; per-worker AI cost could surprise | Medium | Mitigated | Mock by default; the direct-provider seam (`LlmAdapter`/`AIRouter`, [ADR-0008](../decisions/0008-litellm-to-direct-providers.md)) keeps providers/models swappable; measure in staging before enabling. Spend is now bounded on three axes (all fail-closed → mock, all BEFORE the provider call): a **hard per-call ceiling** (`AI_MAX_CALL_COST_INR`), a rolling **per-day + cumulative** cap, a **per-user/day cap** (`AI_MAX_USER_DAILY_COST_INR`, default ₹6/user/day, keyed by opaque worker_ref), and a rolling **retry budget** (cuts the ~9×-per-stuck-job multiplication) — plus an independent **kill-switch** — shipped in `cost_tracker.SpendLedger` ([TD27](./tech-debt-register.md), paying down). Real-LLM canonicalization **validated at 95%** in staging behind the cap (2026-06-15). **Residual (before PROD):** the ledger is per-process, so caps are per-Uvicorn-worker until the shared-store (Redis) follow-up; size caps accordingly and watch `GET /ai/spend` + the `ai.spend_cap_exceeded` event / Langfuse. **Prod-flip threshold (written, numeric) defined 2026-06-15** in [ADR-0008](../decisions/0008-litellm-to-direct-providers.md#staging--prod-real-call-flip-threshold-added-2026-06-15): ≥90% accuracy (95% ✅) AND ≤₹2/worker (≈₹0.27 ✅) AND ≤2% error (0% ✅) AND cap-never-breached/kill-switch-fires (✅) AND 0 PII/over-masking (✅) AND p95 latency ≤8 s (⚠️ unmeasured) AND stable over ≥3 runs/≥150 calls (1/56 so far). **Prod prerequisites:** shared-store (Redis) ledger (caps are per-process today) + secrets manager. Prod stays OFF until met + human-approved. | AI + Performance |
| R7 | **Single point of failure on the AI service** for the profiling flow | Medium | Open | Keep AI path async/queued (BullMQ planned) so API stays up if AI degrades; fail-closed returns safe fallback. | Architect + DevOps |
| R8 | **Secrets sprawl** as real providers are added (.env across apps) | Medium | Open | Typed config split (server/public) already enforced; add a secrets manager before multi-env. | DevOps + Security |
| R9 | **Chat history reached LLM input + Langfuse raw** — `/profiling/respond` pseudonymized only the current message, not prior turns | High (Critical if real calls/Langfuse on) | Closed (2026-06-10) | Found in the Phase-1 AI privacy review; fixed via `_pseudonymized_history()` (history gated + dropped fail-closed) with regression test. See [phase-1-ai-privacy-review.md](../../docs/ai/phase-1-ai-privacy-review.md). | AI + Security |
| R10 | **New `worker-conversations` Storage bucket holds raw conversation JSON** (transcript can contain phone/name/employer); a mis-scoped ACL or un-erased object on consent revoke is a PII exposure | High | Open | Bucket private + backend service-role only (Storage Mode A, no public/anon access — same tier as `voice_notes`); object keys are opaque UUIDs only, enforced fail-closed by `conversationObjectKey` ([@badabhai/validators](../../packages/validators/src/index.ts)); events/ai_jobs/audit_logs reference the path, never the body; per-worker prefix makes DPDP erasure a single sweep. **Open until** the consent-revoke prefix-delete is wired (chat-persistence work). See [ADR-0003](../decisions/0003-worker-conversation-storage-boundary.md). | Security + Backend |
| R11 | **Worker real name (TD21) is now served over unauthenticated endpoints** — `GET /resume/:id` returns the name inside the resume body, and `PUT /workers/:id/name` lets any caller set a worker's name; neither has caller-identity authz (the platform-wide gap, R1/TD4) | Medium | Open | Bounded for now: ids are unguessable UUIDv4; `generated_resumes`/`workers` are RLS+REVOKE-locked at the DB (TD20) so the Data API can't read them; no in-repo client consumes these. The name never reaches the LLM/events/ai_jobs/logs (encrypted at rest, injected into the resume server-side post-AI). **Launch gate:** close caller authz (R1/TD4) before any client-facing exposure of these two routes. The resume renderer (PDF, TD5) now output-encodes (HTML-escapes) the name; the PDF download path is tracked separately in **R13**. **Update (2026-06-12):** `GET /resume/:id` (+ `/:id/download|share|regenerate`) is now behind `InternalServiceGuard` (shared service token, fail-closed). Still open pending TD4: `PUT /workers/:id/name`, and `GET /workers/:id/profile` — which embeds `resume.resumeText` (now name-bearing, TD21) as the ops-console read path (bounded by UUIDv4 + TD20 RLS). **Update (2026-06-15, TD29 G1):** `GET /resume/:id/download` now uses per-worker auth + ownership (see R13) — that route's IDOR/cross-worker gap is closed; `PUT /workers/:id/name` and `GET /workers/:id/profile` remain on the TD4 gap. | Security + Backend |
| R12 | **`voice_notes.transcript_text` is plaintext at rest** — raw worker free-text (can contain phone/name/employer) is stored unencrypted, unlike `phone_e164`/`full_name` (now AES-256-GCM) | Medium | Open | Contained: `voice_notes` is RLS+REVOKE-locked (migration 0009, TD20) so not Data-API readable; no app endpoint returns the transcript; it never enters events/ai_jobs/logs and reaches the LLM only via the pseudonymization gate. **Fix before real voice profiling:** encrypt `transcript_text` with `PiiCryptoService` (same as phone/name). Found in the [PII-at-rest/RLS review](../security/phase-1-pii-at-rest-rls-review.md) (2026-06-12). | Security + Backend |
| R13 | **Resume PDF render serves a worker's real name via signed-URL download** (`GET /resume/:id/download`, TD5/[ADR-0007](../decisions/0007-resume-render-node-boundary.md)) with no caller-identity authz (the R1/TD4 gap) | High | Open | Bounded: UUIDv4 object keys; `RESUMES_BUCKET` is private + backend service-role only; the `generated_resumes` row is RLS+REVOKE-locked (TD20); the signed-URL TTL is ≤15 min (`RESUME_SIGNED_URL_TTL_SECONDS`=900), minted server-side per request, never logged or put in an event; the name is HTML-escaped into the PDF and **renders in Node, never ai-service** (ADR-0007). **Interim mitigation (2026-06-12):** the PII/signed-URL routes (`GET /resume/:id` + `/:id/download|share|regenerate`) are now behind `InternalServiceGuard` (shared `INTERNAL_SERVICE_TOKEN`, fail-closed) so a caller who merely guesses a UUID is denied — service-to-service, NOT per-worker auth. `worker-resumes` is provisioned PRIVATE via [`storage-buckets.sql`](../../infra/supabase/storage-buckets.sql) (idempotent). **Remaining launch gates:** (1) real per-worker authz (R1/TD4) before any client-facing exposure; (2) APPLY the bucket SQL to Supabase + verify `public=false`; (3) DPDP erasure-on-revoke must sweep `resumes/{workerId}/` (like R10). The new `INTERNAL_SERVICE_TOKEN` secret adds to R8/TD10; while TD4 is open, `RESUME_GLOBAL_DAILY_CAP` remains the only spend backstop. **Update (2026-06-15, TD24):** gate (2) **CLEARED + verified** — the `worker-resumes` bucket was created PRIVATE and the live signed-URL suite ([`resume-signed-url.e2e.test.ts`](../../tests/e2e/resume-signed-url.e2e.test.ts)) passed 5/5 against the real project: bucket `public=false`, signed-URL happy path (200/pdf), **both** anon routes denied (public + raw/no-token), and 1s URL expiry. The private bucket is also declared in [`supabase/config.toml`](../../supabase/config.toml). Remaining gates unchanged: (1) per-worker authz (R1/TD4) before client-facing exposure; (3) DPDP erasure-on-revoke sweep. The `RESUME_RENDER_ENABLED=true` app-flip stays operator-run per environment ([runbook](../../infra/supabase/storage-buckets.md)). **Update (2026-06-15, TD29 G1):** the **download path is now closed by per-worker authz** — `GET /resume/:id/download` moved from `InternalServiceGuard` to `WorkerAuthGuard` + an **ownership check** (`resume.workerId === session worker.id`; not-found and not-owner both 404, no existence oracle). The name-bearing PDF can no longer be fetched (or probed) by another worker; authz is now the primary control (UUIDv4 = defense-in-depth). Security gate PASS. **Residual:** `GET /resume/:id` (+ `:id/regenerate`/`:id/share`) still ride `InternalServiceGuard` (TD4), and DB-layer RLS is still the platform gap (TD4/TD20). Ops can no longer mint a download URL — add a separate explicitly-authz'd ops path if that workflow is needed (never re-add the service token here). | Security + Backend |

| R14 | **Boot-time safety gates fail OPEN when `NODE_ENV` is unset** — `nodeEnvSchema` defaults to `development` ([packages/config/src/shared.ts](../../packages/config/src/shared.ts)), so `assertAuthConfig` / `assertPiiCryptoConfig` early-return and permit dev shortcuts (dev `JWT_SECRET`, `SMS_PROVIDER=console` → which now also echoes `dev_otp` in the OTP response, insecure PII keys) if a staging/prod deploy forgets to set `NODE_ENV` | Medium | Closed (2026-06-15) | Pre-existing posture (surfaced 2026-06-15 in the PR #32 auth security gate; **not** introduced by the `dev_otp` seam — that seam is otherwise gated to console mode, which the same assertions forbid outside dev/test). Bounded operationally: deploys set `NODE_ENV` explicitly. **Closed (2026-06-15, `fix/r14-failclose-boot-gates`):** on review the gates were already fail-CLOSED — `assertAuthConfig` / `assertPiiCryptoConfig` read **raw** `process.env.NODE_ENV` (NOT the Zod-parsed value) and treat anything but an explicit `development`/`test` (incl. unset/typo) as non-dev → throw at boot ([main.ts](../../apps/api/src/main.ts) runs both before `NestFactory.create`); the unset case was already covered by tests, so the original "fail OPEN" framing overstated the live risk. The true residual was the **footgun**: the parsed `config.NODE_ENV` defaults to `development` (fail-open) with nothing stopping a *future* gate from keying off it. Hardened by extracting one canonical fail-closed [`isDevEnv()`](../../packages/config/src/shared.ts) that both asserts now route through (single source of truth), a FOOTGUN doc on `nodeEnvSchema` forbidding security use of the parsed value, and a regression test ([`dev-env.test.ts`](../../packages/config/src/dev-env.test.ts)). We deliberately did **not** drop the Zod default (local dev/tests rely on it); the raw-env helper gives the same guarantee without breaking dev boot. Security gate **PASS** — no gate reads the fail-open parsed value. **Operational:** still set `NODE_ENV` explicitly per env. Adjacent pre-existing open-CORS pre-prod gap split out to **[TD30](./tech-debt-register.md)**. | Security + DevOps |

| R15 | **Job-posting ops writes are unauthenticated + the event actor is client-supplied** — `POST/PATCH/POST :id/close` on `/job-postings` ([ADR-0010](../decisions/0010-ops-job-postings-banded-stored-only.md)) have no auth guard, and `created_by` (which becomes `actor.actor_id` on every `job_posting.*` event) arrives in the request body, so the ops audit-spine attribution is forgeable | Medium | Open | **Consistent with existing Phase-1 posture** — actions/workers/events controllers are likewise unguarded and ops auth is deferred (CLAUDE.md §8); not a new class of gap. Bounded: the ops console is internal-only, the events carry **no PII** (ids/enums/booleans/field-keys only — security gate PASS), and `created_by` is an opaque stub UUID, not a person id. **Launch gate (Phase-2, same as R1/TD4):** add an ops auth guard and resolve `actor_id` from the authenticated ops session instead of the body before any non-internal exposure — otherwise `job_posting.*` actor attribution stays spoofable. Surfaced 2026-06-15 by the ADR-0010 security gate (WARNING-1/2); folded with [TD31](./tech-debt-register.md) / [TD4](./tech-debt-register.md). | Security + Backend |

> Add a row the moment a risk is identified. Move to **Mitigated/Closed** with a
> dated note rather than deleting — the history is the value.
