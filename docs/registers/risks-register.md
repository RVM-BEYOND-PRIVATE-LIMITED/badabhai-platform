# Risks Register

Severity: **Critical** (privacy leak / auth bypass / data loss / fail-open AI) ·
**High** · **Medium** · **Low**. Status: **Open** · **Mitigated** · **Accepted** ·
**Closed**.

Seeded 2026-06-09 from ADR-0001, the architecture overview, and the Phase-1 plan.

| ID | Risk | Sev | Status | Mitigation | Owner |
| -- | ---- | --- | ------ | ---------- | ----- |
| R1 | **Supabase RLS not finalized**; API uses the service role, so any accidental client-side or mis-scoped path could read across workers | High | Open | Service role stays server-only; never shipped to web/worker apps. RLS plan in [rls-plan.md](../../infra/supabase/rls-plan.md). Finalize before any direct client→DB access. **Update (2026-06-15, [ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md)):** the two new ops applicant reads (`GET /jobs/:jobId/applicants`, `GET /workers/:workerId/applications`) and the new `jobs`/`applications` tables ride this same `InternalServiceGuard`/service-role posture pending finalised RLS — re-cover them when RLS lands (TD20/Q11). PII is excluded from both projections (`worker_id` + coarse non-PII job fields only), so the gap is **contained, not widened**. | Security + DB |
| R2 | **Pseudonymization is heuristic** (regex + gazetteers); a missed entity could leak PII to an LLM | Critical (if real calls on) | Mitigated | Over-masking bias; **fails closed** on residual digit runs/oversize/parse error; `AI_ENABLE_REAL_CALLS=false` by default; real NER planned. See [pseudonymization.md](../../docs/ai/pseudonymization.md). | AI + Security |
| R3 | **Mock providers** (OTP, STT, LLM, payment) must be replaced before any real launch; easy to forget one | High | Open | Tracked in [tech-debt](./tech-debt-register.md); launch checklist gates each. | DevOps + Backend |
| R4 | **DPDP consent flows are structural placeholders**; production legal copy/flow not done | High | Open | Consent captured structurally now; `docs/legal-later` holds the gap. Launch gate. | Product + Security |
| R5 | **No disaster-recovery runbook / tested restore** for Supabase Postgres yet | Medium | Open | Rely on Supabase managed backups in Phase 1; write + test a [DR runbook](./tech-debt-register.md) before launch. | DevOps |
| R6 | **API-first LLM cost/latency at scale unknown**; per-worker AI cost could surprise | Medium | Mitigated | Mock by default; the direct-provider seam (`LlmAdapter`/`AIRouter`, [ADR-0008](../decisions/0008-litellm-to-direct-providers.md)) keeps providers/models swappable; measure in staging before enabling. Spend is now bounded on three axes (all fail-closed → mock, all BEFORE the provider call): a **hard per-call ceiling** (`AI_MAX_CALL_COST_INR`), a rolling **per-day + cumulative** cap, a **per-user/day cap** (`AI_MAX_USER_DAILY_COST_INR`, default ₹6/user/day, keyed by opaque worker_ref), and a rolling **retry budget** (cuts the ~9×-per-stuck-job multiplication) — plus an independent **kill-switch** — shipped in `cost_tracker.SpendLedger` ([TD27](./tech-debt-register.md), paying down). Real-LLM canonicalization **validated at 95%** in staging behind the cap (2026-06-15). **Residual (before PROD):** the ledger is per-process, so caps are per-Uvicorn-worker until the shared-store (Redis) follow-up; size caps accordingly and watch `GET /ai/spend` + the `ai.spend_cap_exceeded` event / Langfuse. **Prod-flip threshold (written, numeric) defined 2026-06-15** in [ADR-0008](../decisions/0008-litellm-to-direct-providers.md#staging--prod-real-call-flip-threshold-added-2026-06-15): ≥90% accuracy (95% ✅) AND ≤₹2/worker (≈₹0.27 ✅) AND ≤2% error (0% ✅) AND cap-never-breached/kill-switch-fires (✅) AND 0 PII/over-masking (✅) AND p95 latency ≤8 s (⚠️ unmeasured) AND stable over ≥3 runs/≥150 calls (1/56 so far). **Prod prerequisites:** shared-store (Redis) ledger (caps are per-process today) + secrets manager. Prod stays OFF until met + human-approved. | AI + Performance |
| R7 | **Single point of failure on the AI service** for the profiling flow | Medium | Open | Keep AI path async/queued (BullMQ planned) so API stays up if AI degrades; fail-closed returns safe fallback. | Architect + DevOps |
| R8 | **Secrets sprawl** as real providers are added (.env across apps) | Medium | Open | Typed config split (server/public) already enforced; add a secrets manager before multi-env. | DevOps + Security |
| R9 | **Chat history reached LLM input + Langfuse raw** — `/profiling/respond` pseudonymized only the current message, not prior turns | High (Critical if real calls/Langfuse on) | Closed (2026-06-10) | Found in the Phase-1 AI privacy review; fixed via `_pseudonymized_history()` (history gated + dropped fail-closed) with regression test. See [phase-1-ai-privacy-review.md](../../docs/ai/phase-1-ai-privacy-review.md). | AI + Security |
| R10 | **New `worker-conversations` Storage bucket holds raw conversation JSON** (transcript can contain phone/name/employer); a mis-scoped ACL or un-erased object on consent revoke is a PII exposure | High | Open | Bucket private + backend service-role only (Storage Mode A, no public/anon access — same tier as `voice_notes`); object keys are opaque UUIDs only, enforced fail-closed by `conversationObjectKey` ([@badabhai/validators](../../packages/validators/src/index.ts)); events/ai_jobs/audit_logs reference the path, never the body; per-worker prefix makes DPDP erasure a single sweep. **Open until** the consent-revoke prefix-delete is wired (chat-persistence work). See [ADR-0003](../decisions/0003-worker-conversation-storage-boundary.md). | Security + Backend |
| R11 | **Worker real name (TD21) is now served over unauthenticated endpoints** — `GET /resume/:id` returns the name inside the resume body, and `PUT /workers/:id/name` lets any caller set a worker's name; neither has caller-identity authz (the platform-wide gap, R1/TD4) | Medium | Open | Bounded for now: ids are unguessable UUIDv4; `generated_resumes`/`workers` are RLS+REVOKE-locked at the DB (TD20) so the Data API can't read them; no in-repo client consumes these. The name never reaches the LLM/events/ai_jobs/logs (encrypted at rest, injected into the resume server-side post-AI). **Launch gate:** close caller authz (R1/TD4) before any client-facing exposure of these two routes. The resume renderer (PDF, TD5) now output-encodes (HTML-escapes) the name; the PDF download path is tracked separately in **R13**. **Update (2026-06-12):** `GET /resume/:id` (+ `/:id/download|share|regenerate`) is now behind `InternalServiceGuard` (shared service token, fail-closed). Still open pending TD4: `PUT /workers/:id/name`, and `GET /workers/:id/profile` — which embeds `resume.resumeText` (now name-bearing, TD21) as the ops-console read path (bounded by UUIDv4 + TD20 RLS). **Update (2026-06-15, TD29 G1):** `GET /resume/:id/download` now uses per-worker auth + ownership (see R13) — that route's IDOR/cross-worker gap is closed; `PUT /workers/:id/name` and `GET /workers/:id/profile` remain on the TD4 gap. | Security + Backend |
| R12 | **`voice_notes.transcript_text` is plaintext at rest** — raw worker free-text (can contain phone/name/employer) is stored unencrypted, unlike `phone_e164`/`full_name` (now AES-256-GCM) | Medium | Open | Contained: `voice_notes` is RLS+REVOKE-locked (migration 0009, TD20) so not Data-API readable; no app endpoint returns the transcript; it never enters events/ai_jobs/logs and reaches the LLM only via the pseudonymization gate. **Fix before real voice profiling:** encrypt `transcript_text` with `PiiCryptoService` (same as phone/name). Found in the [PII-at-rest/RLS review](../security/phase-1-pii-at-rest-rls-review.md) (2026-06-12). | Security + Backend |
| R13 | **Resume PDF render serves a worker's real name via signed-URL download** (`GET /resume/:id/download`, TD5/[ADR-0007](../decisions/0007-resume-render-node-boundary.md)) with no caller-identity authz (the R1/TD4 gap) | High | Open | Bounded: UUIDv4 object keys; `RESUMES_BUCKET` is private + backend service-role only; the `generated_resumes` row is RLS+REVOKE-locked (TD20); the signed-URL TTL is ≤15 min (`RESUME_SIGNED_URL_TTL_SECONDS`=900), minted server-side per request, never logged or put in an event; the name is HTML-escaped into the PDF and **renders in Node, never ai-service** (ADR-0007). **Interim mitigation (2026-06-12):** the PII/signed-URL routes (`GET /resume/:id` + `/:id/download|share|regenerate`) are now behind `InternalServiceGuard` (shared `INTERNAL_SERVICE_TOKEN`, fail-closed) so a caller who merely guesses a UUID is denied — service-to-service, NOT per-worker auth. `worker-resumes` is provisioned PRIVATE via [`storage-buckets.sql`](../../infra/supabase/storage-buckets.sql) (idempotent). **Remaining launch gates:** (1) real per-worker authz (R1/TD4) before any client-facing exposure; (2) APPLY the bucket SQL to Supabase + verify `public=false`; (3) DPDP erasure-on-revoke must sweep `resumes/{workerId}/` (like R10). The new `INTERNAL_SERVICE_TOKEN` secret adds to R8/TD10; while TD4 is open, `RESUME_GLOBAL_DAILY_CAP` remains the only spend backstop. **Update (2026-06-15, TD24):** gate (2) **CLEARED + verified** — the `worker-resumes` bucket was created PRIVATE and the live signed-URL suite ([`resume-signed-url.e2e.test.ts`](../../tests/e2e/resume-signed-url.e2e.test.ts)) passed 5/5 against the real project: bucket `public=false`, signed-URL happy path (200/pdf), **both** anon routes denied (public + raw/no-token), and 1s URL expiry. The private bucket is also declared in [`supabase/config.toml`](../../supabase/config.toml). Remaining gates unchanged: (1) per-worker authz (R1/TD4) before client-facing exposure; (3) DPDP erasure-on-revoke sweep. The `RESUME_RENDER_ENABLED=true` app-flip stays operator-run per environment ([runbook](../../infra/supabase/storage-buckets.md)). **Update (2026-06-15, TD29 G1):** the **download path is now closed by per-worker authz** — `GET /resume/:id/download` moved from `InternalServiceGuard` to `WorkerAuthGuard` + an **ownership check** (`resume.workerId === session worker.id`; not-found and not-owner both 404, no existence oracle). The name-bearing PDF can no longer be fetched (or probed) by another worker; authz is now the primary control (UUIDv4 = defense-in-depth). Security gate PASS. **Residual:** `GET /resume/:id` (+ `:id/regenerate`/`:id/share`) still ride `InternalServiceGuard` (TD4), and DB-layer RLS is still the platform gap (TD4/TD20). Ops can no longer mint a download URL — add a separate explicitly-authz'd ops path if that workflow is needed (never re-add the service token here). | Security + Backend |

| R14 | **Boot-time safety gates fail OPEN when `NODE_ENV` is unset** — `nodeEnvSchema` defaults to `development` ([packages/config/src/shared.ts](../../packages/config/src/shared.ts)), so `assertAuthConfig` / `assertPiiCryptoConfig` early-return and permit dev shortcuts (dev `JWT_SECRET`, `SMS_PROVIDER=console` → which now also echoes `dev_otp` in the OTP response, insecure PII keys) if a staging/prod deploy forgets to set `NODE_ENV` | Medium | Closed (2026-06-15) | Pre-existing posture (surfaced 2026-06-15 in the PR #32 auth security gate; **not** introduced by the `dev_otp` seam — that seam is otherwise gated to console mode, which the same assertions forbid outside dev/test). Bounded operationally: deploys set `NODE_ENV` explicitly. **Closed (2026-06-15, `fix/r14-failclose-boot-gates`):** on review the gates were already fail-CLOSED — `assertAuthConfig` / `assertPiiCryptoConfig` read **raw** `process.env.NODE_ENV` (NOT the Zod-parsed value) and treat anything but an explicit `development`/`test` (incl. unset/typo) as non-dev → throw at boot ([main.ts](../../apps/api/src/main.ts) runs both before `NestFactory.create`); the unset case was already covered by tests, so the original "fail OPEN" framing overstated the live risk. The true residual was the **footgun**: the parsed `config.NODE_ENV` defaults to `development` (fail-open) with nothing stopping a *future* gate from keying off it. Hardened by extracting one canonical fail-closed [`isDevEnv()`](../../packages/config/src/shared.ts) that both asserts now route through (single source of truth), a FOOTGUN doc on `nodeEnvSchema` forbidding security use of the parsed value, and a regression test ([`dev-env.test.ts`](../../packages/config/src/dev-env.test.ts)). We deliberately did **not** drop the Zod default (local dev/tests rely on it); the raw-env helper gives the same guarantee without breaking dev boot. Security gate **PASS** — no gate reads the fail-open parsed value. **Operational:** still set `NODE_ENV` explicitly per env. Adjacent pre-existing open-CORS pre-prod gap split out to **[TD30](./tech-debt-register.md)** — now **CLOSED (2026-06-22, `fix/td30-cors-allowlist`):** the open `app.enableCors()` is replaced by an env-driven (`CORS_ALLOWED_ORIGINS`), fail-closed allow-list ([`assertCorsConfig`](../../packages/config/src/server.ts) throws at boot on an empty list outside dev/test, routed through the same canonical [`isDevEnv()`](../../packages/config/src/shared.ts) raw-`NODE_ENV` rule as the R14 gates), so the open-CORS browser-facing-API vuln is mitigated (transport-only; no schema/event/PII change). | Security + DevOps |

| R15 | **`feed.shown` is one event per impression per fetch** ([ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) R-A) — the alpha swipe feed emits one `feed.shown` per surfaced job on every load, which can grow the `events` table on repeated feed loads | Low | Mitigated | Bounded per ADR: the feed serves a small page (`limit` 1..50, default 20) and emits one event per returned item, so per-fetch volume is capped. `feed.shown` is intentionally **unkeyed** (no idempotency dedupe) for alpha. PII-free (`worker_id` + opaque `job_id` + ranking-signal defaults). **Revisit** a per-`(worker, job, day)` idempotencyKey to collapse repeat impressions if volume warrants — not a blocker at alpha volumes. | Backend |
| R23 | **Job-posting ops writes are unauthenticated + the event actor is client-supplied** — `POST/PATCH/POST :id/close` on `/job-postings` ([ADR-0012](../decisions/0012-ops-job-postings-banded-stored-only.md)) have no auth guard, and `created_by` (which becomes `actor.actor_id` on every `job_posting.*` event) arrives in the request body, so the ops audit-spine attribution is forgeable | Medium | Open | **Consistent with existing Phase-1 posture** — actions/workers/events controllers are likewise unguarded and ops auth is deferred (CLAUDE.md §8); not a new class of gap. Bounded: the ops console is internal-only, the events carry **no PII** (ids/enums/booleans/field-keys only — security gate PASS), and `created_by` is an opaque stub UUID, not a person id. **Launch gate (Phase-2, same as R1/TD4):** add an ops auth guard and resolve `actor_id` from the authenticated ops session instead of the body before any non-internal exposure — otherwise `job_posting.*` actor attribution stays spoofable. Surfaced 2026-06-15 by the ADR-0012 security gate (WARNING-1/2); folded with [TD34](./tech-debt-register.md) / [TD4](./tech-debt-register.md). *(R22 = the sibling Reach-feed PR's unauth-endpoints risk.)* | Security + Backend |
| R22 | **Reach serving endpoints are unauthenticated** — `GET /reach/jobs/:jobId/applicants` and `GET /reach/workers/:workerId/feed` ([ADR-0011](../decisions/0011-reach-feed-serving.md)) have no auth guard; View A returns the **full ranked worker pool** for any jobId, View B the candidate-job set for any workerId | Medium | Open | **Consistent with existing Phase-1 ops posture** — the read controllers (actions/workers/events) are likewise unguarded; ops auth is deferred (CLAUDE.md §8, RLS not finalized R1/TD4). Strongly bounded by **faceless** output: responses + the `feed.shown` events carry only opaque `worker_id`/`job_id`, numeric `score`, booleans, and the explainable `components[]` — **no name/phone/employer/contact** (the module never reads `workers`; the repository projects signal columns only, never `embedding`/`rawProfile`; security gate PASS). Residual exposure is the *existence + relative ranking* of the pool to anyone who reaches the API. **Launch gate (Phase-2, same as R1/TD4):** gate both routes behind the ops auth guard when it lands, before any non-internal exposure. Surfaced 2026-06-15 by the ADR-0011 security gate (W1). **Update (2026-06-20, ADR-0019 R22 / PR2 `feat/r22-payer-reach-view`):** the EXTERNAL self-serve reach surface now ships as a NEW guarded route group `GET /payer/reach/jobs/:jobId/applicants` behind `PayerAuthGuard`, scoped to the payer's OWNED `jobs` (`jobs.payer_id == session payer`) with a no-oracle identical-404 ownership read (a payer can't read or enumerate other payers' jobs), faceless rows + payer-free `feed.shown`, and a per-payer reach rate cap (fail-closed). **bb-security-review + independent authz-review PASS** ([threat-model addendum](../security/payer-reach-view-threat-model-addendum.md)). **The ops `/reach/*` endpoints stay UNAUTHENTICATED by design** (one principal per route — ops posture unchanged); this risk's ORIGINAL scope (the ops endpoints) therefore stays **Open** as the launch-gated ops residual, with DB-RLS (XL-A) + the ops auth guard the remaining gates. | Security + Backend |

| R16 | **Contact Unlock rides `InternalServiceGuard` — no per-payer identity** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §6.3/F-7, [threat model](../security/contact-unlock-threat-model.md) T7/RR-1) — the Stream-A unlock/reveal routes take `payer_id` in the request body and have no per-payer authz, so "this payer owns this unlock" is **unenforceable**; any holder of the shared secret can act as any `payer_id` | High | **Closed/Mitigated for the EXTERNAL self-serve surface (2026-06-20, [ADR-0019](../decisions/0019-self-serve-payer-portal.md) Phase 1, PR `feat/r16-lc1-payer-auth-close`); open-GA launch gates (DB-RLS XL-A / real payments / DPDP) remain RESIDUAL** | Contained in alpha: the only caller is the trusted backend/ops secret-holder, and the worker-protection caps + `employer_sharing` consent gate + no-oracle responses still apply (even the secret-holder cannot exceed caps or reach a non-consenting worker). The High finding **F-A** (unknown-worker FK-500 oracle) found in the bb-security-review is **CLOSED** — fixed + has a CI regression test. **Launch gate (LC-1):** **NO production / client-facing payer surface may ship until a `PayerAuthGuard` (per-payer identity + authz to `payer_id`) + a horizontal-authz test (payer A cannot act on payer B's `payer_id`) land.** Interim posture is the same family as the service-role gap — cross-link R1/R11 and [TD33](./tech-debt-register.md). **Update (2026-06-20, ADR-0019 Phase 1 PR1 — LC-1 SATISFIED for the payer-self surface):** `PayerAuthGuard` is built + wired; self-serve **login** + **XB-G/XB-H** land; **bb-security-review + independent security-engineer PASS.** **Update (2026-06-20, ADR-0019 Phase 1 PR `feat/r16-lc1-payer-auth-close` — R16/LC-1 CLOSED for the external surface):** the CANONICAL `/unlocks` + `/payers/:payerId/credits` controller ([`unlocks.controller.ts`](../../apps/api/src/unlocks/unlocks.controller.ts)) was **retrofitted IN-PLACE** from `InternalServiceGuard` onto `PayerAuthGuard` — `payer_id` is now the **verified session** (the body/`:payerId` param is no longer trusted; `assertPayerOwns` blocks cross-payer credit purchase/read, XB-A), reveal/`getOwn` are no-oracle (not-owned/unknown → identical neutral body, never a 403/404 oracle), and the per-payer **XB-G** cap runs before the chokepoint. The interim parallel `/payer/unlocks` controller was **RETIRED** — one self-serve disclosure surface, no body `payer_id` anywhere. App-layer tenancy is the enforced control, with the horizontal-authz tests as the build-blocker ([`unlocks.controller.test.ts`](../../apps/api/src/unlocks/unlocks.controller.test.ts) XB-A: payer A cannot read/buy/reveal against payer B's id). **The ops `InternalServiceGuard` `/unlocks*` routes are GONE from this surface** (the canonical routes are now payer-principal-only; one principal per route). **bb-security-review gate the pre-merge bar.** **Residual (open-GA launch gates, NOT closed):** DB-enforced RLS (XL-A / R20) is the defense-in-depth upgrade external access forces; real payments (R17/TD34) and production DPDP/DPA copy (R19/TD35) stay human-gated — closed-beta/staging only until all clear. MOCK + STAGING-ONLY. Cross-link [TD33](./tech-debt-register.md). | Security + Backend |
| R17 | **Real payments are off (`PAYMENTS_ENABLE_REAL=false`)** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D5, [threat model](../security/contact-unlock-threat-model.md) T6/LC-5) — alpha is a mock credit ledger only; enabling a real gateway (Razorpay) means real keys + real money movement | High | Open — **launch gate** | Mock-only by default behind the `PaymentGateway` seam with a fail-closed config assertion (mirrors `AI_ENABLE_REAL_CALLS`/`assertPiiCryptoConfig`); every `payment.*` event carries `real_call: false` so ops can prove no real money moved. **Human-gated escalation:** real Razorpay keys/spend require explicit human sign-off, staging-first, separate from this stream (CLAUDE.md §7). Tracked for payback in [TD34](./tech-debt-register.md). | Backend + DevOps |
| R18 | **Raw-phone reveal / real telephony provider is out of alpha** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D2, [threat model](../security/contact-unlock-threat-model.md) T8/LC-4/LC-6) — alpha reveal is **in-app relay only** (no number leaves BadaBhai); a real masked-number/telephony provider, or a raw-phone reveal, would expose the raw number to a new external party | High | Open — **launch gate** | Alpha discloses no number at all — the in-app relay keeps the raw phone inside BadaBhai, read transiently only at the routed-reveal step (`PiiCryptoService.decrypt`), never persisted/returned/logged/in an event. **Human-gated escalations:** (a) selecting/funding a real telephony/proxy provider → STOP + escalate + DPA + staging-first + flag-gated; (b) any raw-phone reveal → a *separate* higher-tier consent + a team decision + a fresh threat-model addendum (default: never in alpha). Both are launch gates (CLAUDE.md §7). | Security + Backend |
| R19 | **DPDP lawful-basis + notice copy for `employer_sharing`, and a retention/erasure policy for routing tokens + the worker↔payer linkage, are not done** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D3/R-3, [threat model](../security/contact-unlock-threat-model.md) T10/LC-2/LC-3) — disclosure to a payer is a *distinct* DPDP purpose; the production notice copy + lawful-basis wording and the retention/erasure story (expired `unlock_routing` purge; `unlocks`/credit-row retention window; crypto-shred of the worker↔payer linkage) are unwritten | High | Open — **launch gate** | The *mechanism* is in place (a separate, append-only, versioned, revocable `employer_sharing` consent purpose; a fail-closed disclosure gate; PII-free linkage tables); only the legal **copy/basis** and the written **retention/erasure policy** remain. **No non-mock disclosure** until the `employer_sharing` notice copy lands with the production DPDP legal-copy stream and the retention policy is written. Cross-link the existing DPDP-copy risk R4 and [TD35](./tech-debt-register.md). | Product + Security |
| R20 | **RLS for the four new unlock tables not finalized** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §6.1/R-2, [threat model](../security/contact-unlock-threat-model.md) T9/RR-2) — `unlocks` / `payer_credits` / `credit_ledger` / `unlock_routing` ride the service-role posture like the rest of the spine; finalized RLS policies are pending | Medium | Open | Locked today via REVOKE (migration 0014, TD20-style ENABLE+FORCE+REVOKE from anon/authenticated/service_role/PUBLIC); all four tables are **PII-free** (the only identity join is `unlocks.worker_id` → the RLS-locked `workers`), so a Data-API read sees ids/amounts/enums only — never a contact. `unlock_routing` is schema-proven phone-free (T9-a). **Clear when RLS lands:** add the four tables to the [rls-plan](../../infra/supabase/rls-plan.md) + the ADR-0004 `SET ROLE`-denied regression. Cross-link [TD20](./tech-debt-register.md)/R1/[Q11](./open-questions.md). | Security + DB |
| R21 | **No-oracle neutral-response path is not timing-normalized** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) F-3, [threat model](../security/contact-unlock-threat-model.md) T2-c/RR-4/LC-7) — the deny branches (no-consent / capped / unknown / already-owned / expired / over-cap / zero-balance) return a byte-identical neutral body, but do differing work (consent lookup vs cap aggregation vs decrypt+relay), so response **timing** can re-introduce a re-identification oracle the body closes | Medium | Open — **launch gate** (before any real payer surface) | Acceptable for the trusted-shared-secret alpha (the only caller is the backend/ops secret-holder, who has no incentive to time-probe — R16). The response **body + status** are already byte-identical across every deny state via a single neutral-response constructor (F-3, with a table-driven test); only latency is unnormalized. **Launch gate (LC-7):** latency-normalize the neutral path before any per-payer surface ships. | Security + Backend |

> Add a row the moment a risk is identified. Move to **Mitigated/Closed** with a
> dated note rather than deleting — the history is the value.
