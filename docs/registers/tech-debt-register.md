# Technical Debt Register

A deliberate shortcut is fine **if it's logged with a payback trigger**. An
unlogged shortcut is a bug waiting to surprise someone. Status: **Open** ·
**Paying down** · **Paid**.

Seeded 2026-06-09 from the Phase-1 sprint plan and ADR-0001.

| ID | Debt | Why it exists | Payback trigger | Status |
| -- | ---- | ------------- | --------------- | ------ |
| TD1 | ~~Extraction & transcription run inline on the request, not as BullMQ jobs~~ | Phase-1 simplicity | Before enabling real STT/LLM | **Paying down** — extraction ([ADR-0002](../decisions/0002-async-extraction-and-action-recording.md)) and now transcription both async via BullMQ (`voice-transcription` queue, `POST /voice/transcribe` → 202 + poll) |
| TD2 | **Mock OTP** for worker identity | No real provider wired yet | Before onboarding real workers | Open |
| TD3 | **Pseudonymization is heuristic** (regex + gazetteers), not NER | Fast, safe-by-over-masking start | Before enabling real LLM in production | Open |
| TD4 | **RLS not enforced platform-wide** (backend connects as the `postgres` role / BYPASSRLS — NOT the PostgREST `service_role`) | RLS plan not finalized | Before any direct client→DB access or multi-tenant exposure | **Paying down** — `workers` locked: RLS enabled + FORCE + grants revoked from anon/authenticated/**service_role**/PUBLIC, and the phone encrypted at rest ([ADR-0004](../decisions/0004-pii-at-rest-and-rls.md), migrations 0003/0004; Data API GET verified HTTP 403). Rest of the spine now locked the same way → **TD20** (migration 0009, paying down). Remaining: the backend still connects as `postgres`/BYPASSRLS rather than a least-privilege app role |
| TD5 | **Resume generation is a placeholder** (templated) | Phase-1 stub | When real resume output is a product requirement | **Paying down** — no longer name-less: the resume now carries the worker's real name (TD21, injected server-side post-AI so it never reaches the LLM). The body is still a template built from the structured profile — real LLM-authored prose remains gated behind `AI_ENABLE_REAL_CALLS`. **Layer 2 (PDF render) built:** an async BullMQ `resume-render` worker turns the canonical profile → HTML → **WeasyPrint PDF** (rendered in **Node, never ai-service** — [ADR-0007](../decisions/0007-resume-render-node-boundary.md)) → a private Supabase Storage bucket, downloaded by short-TTL signed URL; pre-generated on `profile.confirmed`; `resume.generated`/`downloaded`/`regenerated`/`shared` events wired; daily rate-cap (per-worker + global, fail-closed). Gated behind `RESUME_RENDER_ENABLED` (off by default; degrades to a name-less / no-PDF path when off or the binary is absent). Phase-1 deferrals → **TD24** |
| TD6 | **Sarvam STT real call is a placeholder** (the adapter is wired but the real provider call is gated off) | Real STT needs a DPDP/spend decision | When voice profiling becomes a real flow | **Paying down** — async transcription end-to-end on the gated, mock-by-default path (`apps/ai-service/app/stt.py`, `POST /voice/transcribe`, BullMQ processor, events). Only the real Sarvam call in `_transcribe_real` remains, fail-closed behind `AI_ENABLE_REAL_CALLS` + `SARVAM_API_KEY` |
| TD7 | ~~**Flutter CI job is `continue-on-error`**~~ | Scaffold authored without a local SDK | Once the app is validated on a real SDK | **Paid (2026-06-15)** — `flutter analyze` + `flutter test` are now a **BLOCKING** gate in a dedicated, path-filtered workflow ([`.github/workflows/worker-app.yml`](../../.github/workflows/worker-app.yml)) that runs only on changes under `apps/worker-app`; the old non-blocking job was removed from `ci.yml`. Flutter pinned to **3.27.4** (stable) for reproducibility. `continue-on-error` is gone → a red check blocks merge. The scaffold's Dart was confirmed lint-clean and the one widget test (`test/widget_test.dart`) gates too. |
| TD8 | ~~`@badabhai/reach-engine` is an **empty placeholder package**~~ | Phase 2+ feature | Start of Reach Engine work | **Paying down** — the deterministic RANK core is implemented ([ADR-0006](../decisions/0006-reach-foundation-rank-core.md)): `scoreWorkerForJob` / `rankWorkersForJob` (sort-never-block, hot-tag gated on real candidates, partial-profile tolerant, deterministic) + PII-free `feed.*`/`application.*` event contracts. **Hardened** against adversarial input: garbage numeric/geo signals (NaN/±Infinity) are treated as UNKNOWN (neutral, §3) not a penalty, and adversarial `RankOptions` are sanitized — proven by a seeded property/invariant suite (`reach-engine.property.test.ts`: bounds, sort-never-block, determinism, hot-gating, huge-fleet). Phase-2 surfaces (job entity, feed, unlock/payments, PACE/PROTECT/LEARN) remain. |
| TD9 | **No coverage threshold** in CI | Suite still young | When the suite is mature enough to set a floor | **Open** — **Update (2026-06-18, apps/api audit verification):** the controller-test gap is **closed** (PR #95 — all 20 controllers now have handler-executing tests; `guard-contract.test.ts` pins authz incl. the consent-gated guard ORDER). Measured `apps/api` (v8): **controllers 100% lines, services+controllers 87.46%, repo-wide 76.52%**. Repo-wide <80% is dragged by deliberately-thinner layers — repositories, BullMQ processors, and `ai.service.ts` (the FastAPI HTTP client, 0%). Remaining: (i) cover repositories/processors/`ai.service.ts` to lift repo-wide ≥80%; (ii) pin a `vitest` coverage threshold in CI (the actual TD9 item) so the floor can't regress. Owner Backend + QA |
| TD10 | **No secrets manager**; secrets live in `.env` | Single-dev Phase-1 setup | Before multi-environment / multiple engineers handle prod secrets | Open |
| TD11 | **Action `context` PII guard is best-effort** (phone/email regex on keys+values); names/addresses not detectable | `context` is for non-PII signals, not free text | If clients start putting free text in `context` — tighten to an allow-list/enum | Open (ADR-0002) |
| TD12 | **Profile-extraction worker runs in-process** (same Nest app) | Phase-1 simplicity; queue boundary already in place | When extraction load needs independent scaling | Open (ADR-0002) |
| TD13 | ~~**Flutter `ApiClient` async-extract change is UNVERIFIED** (no Flutter SDK in build env)~~ | Toolchain absent locally | Run `flutter analyze` + `flutter test` in CI before the app ships | **Paid (2026-06-15)** — the blocking worker-app workflow (TD7) now runs `flutter analyze` + `flutter test` on a pinned SDK on every PR touching `apps/worker-app`, so the `ApiClient` (and the rest of the scaffold) is verified in CI. |
| TD14 | ~~**Partial-success retry can duplicate a profile** (create OK, then markCompleted fails → retry re-creates); idempotency guard only covers already-`completed` redelivery~~ | Full fix needs an `ai_job_id` column on `worker_profiles` or a txn | Before high extraction volume / real LLM | **Paying down** — `worker_profiles.ai_job_id` + UNIQUE index (migration 0007); `ProfilesRepository.create` is now `ON CONFLICT (ai_job_id) DO NOTHING` → returns the existing profile, so a partial-success retry / stalled-job redelivery converges on one row (also handles concurrent redelivery, which a txn alone would not). Proven by `tests/e2e/profile-idempotency.e2e.test.ts`. NULL `ai_job_id` (legacy/non-extraction) still inserts. (ADR-0002) |
| TD15 | **`GET /ai-jobs/:id` passes `output_ref` through untyped + no authz** | Only `{profile_id}` today; authz is the platform-wide RLS gap (TD4) | Type the projection when a 2nd job type lands; close authz with TD4 | Open |
| TD16 | **Root `clean` script (`rimraf … *.tsbuildinfo`) fails on Windows** (glob not enabled) + `nest-cli deleteOutDir` + stale `.tsbuildinfo` can empty `dist` | rimraf 6 needs `--glob`; incremental tsc emits nothing after wipe | Quick fix: `rimraf --glob` or split the patterns | Open |
| TD17 | **TS mock question bank (`apps/api/src/ai/mock-interview.ts`) duplicates the Python `question_bank.py`/`interview_engine.py`** — both the DATA (topics, ACK/WRAP_UP, essential topics) AND the *readiness-trigger semantics*: the mock marks only the one previously-asked topic answered per turn, while the Python engine derives `answered_topics` from message signals (so a rich message can flip `extraction_ready` earlier / multi-topic). e2e runs the mock fallback, so "emit once on flip" is proven for the mock path only. | Mock fallback must advance the interview offline without the Python service; no shared source/codegen | When the question bank changes, or when a shared role/question config table is introduced — keep the two in sync (data + readiness rule) or generate one from the other | Open |
| TD18 | ~~**Event emission is not idempotent under at-least-once retry** — a request that retries after an `events` insert commits but before the turn finishes re-emits the same event (`profile.extraction_ready`, `chat.message_sent`, …); `createEvent` mints a fresh `event_id` and there is no dedup constraint. The `extraction_ready_emitted` marker only stops re-emit on *later* turns, not same-turn retry.~~ | Per-turn writes are non-transactional platform-wide; not unique to one emitter | Platform fix, not per-emitter: a caller-supplied dedup key + unique constraint | **Paying down** — `events.idempotency_key` + a plain UNIQUE index (migration 0006; Postgres `NULLS DISTINCT` → unkeyed events still insert, keyed events dedup); `EventsService.emit/emitMany` thread a PII-free `<event_name>:<uuid>` key and the repository inserts `ON CONFLICT (idempotency_key) DO NOTHING`, so a same-turn retry converges on one row (logs `DEDUPED`). Keyed at every retry-prone emit site (chat/profiles/extraction/voice/consent/resume/auth); `worker.otp_*` + `action.recorded` intentionally unkeyed. Proven by `tests/e2e/events-idempotency.e2e.test.ts`. (escalated from code review) |
| TD19 | **`ProfileExtractionReadyPayload.answered_topics` accepts 40-char free-form strings** (`z.string().min(1).max(40)`×50) and `conversation_state` is loose JSONB (`collected: z.record(z.unknown())`) | Frozen v1 contract; engine only ever emits short controlled topic ids today | Tighten `answered_topics` to an enum / `^[a-z_]+$` so the event boundary rejects non-topic strings; keep the e2e no-PII-in-`conversation_state` assertion as the JSONB guard | Open |
| TD20 | ~~**RLS/REVOKE hardening covers only `workers`** — the rest of the spine (`events`, `ai_jobs`, `audit_logs`, `worker_consents`, `worker_profiles`, `chat_sessions`, `chat_messages`, `voice_notes`, `generated_resumes`) still has default Supabase grants + no RLS, so `phone_hash`/`worker_id`/correlation are readable via the Data API with the anon/service-role key~~ | The change was scoped to the PII table (`workers`) per the request; these tables are PII-free-by-design but still linkable | Extend ENABLE RLS + FORCE + REVOKE to every table + an e2e assertion per table | **Paying down** — migration **0009** applies the `workers` lock (ENABLE + FORCE RLS + REVOKE ALL from PUBLIC/anon/authenticated/service_role) to all **13** remaining tables (the 9 spine tables above + the 4 questionnaire tables `profiles`/`questions`/`profile_questions`/`worker_answers` from 0008). Now the **entire** public schema denies the Data-API roles; backend `postgres`/BYPASSRLS unaffected (same proven setup as `workers`). Proven by `tests/e2e/rls-spine.e2e.test.ts` — per-table `SET ROLE <role>` → SELECT → 42501 across all 14 tables × 3 roles, plus a backend-can-still-read sanity check. ([ADR-0004](../decisions/0004-pii-at-rest-and-rls.md)) |
| TD21 | ~~**`workers.full_name` is plaintext PII** — co-located with the (now-encrypted) phone~~ | No write site yet (nullable, unused in Phase 1) | Encrypt `full_name` with `encryptPii` BEFORE any code writes a real name | **Paying down** — the first name-writer now encrypts at rest: `PUT /workers/:id/name` → `WorkersService.setFullName` runs `PiiCryptoService.encrypt` (AES-256-GCM `v1.` token, same as `phone_e164`) before `WorkersRepository.updateFullName`; the plaintext name is never logged, returned, or put in an event (`worker.name_recorded` carries only `worker_id`). Read-back: the resume injects the decrypted name **server-side, after** the AI call, so it never reaches the LLM. `name_hash` deliberately skipped (no name-search need yet — re-open if it lands). Proven by `workers.service.test.ts`, `resume.service.test.ts`, and the phase-1 e2e (full_name is a `v1.` token; name on the resume; absent from every event/ai_job). ([ADR-0004](../decisions/0004-pii-at-rest-and-rls.md)) |
| TD22 | **No PII key-rotation story** — `encryptPii` token has a format version (`v1`) but no key id, and `PII_HASH_PEPPER` is the dedup key on `phone_hash` so rotating it invalidates every hash | Single-key Phase-1 setup | Add a `kid` to the token + a key map for staged AES rotation + a re-encrypt backfill; treat the pepper as a long-lived secret (rotation = full rehash from decrypted phones). Document a runbook + DPDP crypto-shred (drop key) path | Open ([ADR-0004](../decisions/0004-pii-at-rest-and-rls.md)) |
| TD23 | ~~**`verifyOtp` is check-then-insert** (`findByPhoneHash` then `create`) with no 23505 handling — concurrent double-login can 500; dedup now rests solely on `workers_phone_hash_uq` (the plaintext index was dropped)~~ | Pre-existing; data integrity holds (the unique index prevents dup rows) | `insert … on conflict (phone_hash) do nothing returning`, or catch 23505 + re-read by `phone_hash` | **Paid (2026-06-12)** — new `WorkersRepository.createOrGetByPhoneHash` does an atomic `insert … on conflict (phone_hash) do nothing returning`; on an empty return (lost race) it re-reads by `phone_hash` and reports `created: false`. `verifyOtp` keeps the read-first fast path for the common returning-worker case, then routes the read-miss through this method and gates the one-time `worker.created` event on `created` — so a concurrent first-time login can no longer 500 **or** double-emit `worker.created`. Proven by `auth.service.test.ts` (new lost-race case) — 4 tests. |
| TD24 | **Resume-render Phase-1 deferrals** — (a) the renderer's `tradeContent` block is empty (rich per-trade content not yet wired from the questionnaire output); (b) the daily rate-cap has no per-IP/per-account dimension, so while TD4 is open a caller can rotate `worker_id` to dodge the per-worker cap (only `RESUME_GLOBAL_DAILY_CAP` bounds spend); (c) `RESUMES_BUCKET` (`worker-resumes`) must be created **PRIVATE** out-of-band — RLS/migration 0009 does not cover Storage object ACLs; (d) the API container must have the `weasyprint` binary installed for real rendering (else it degrades to no-PDF) | Built the render worker Node-side under a kill-switch ([ADR-0007](../decisions/0007-resume-render-node-boundary.md)); the rich-content + auth + infra pieces are follow-ups | (a) ✅ trade content wired ([`trade-content.ts`](../../apps/api/src/resume/trade-content.ts), TD24a) → headline/summary/responsibilities; (b) ✅ per-IP hourly cap added ([`ip-rate-limit.service.ts`](../../apps/api/src/common/rate-limit/ip-rate-limit.service.ts), hashed-IP, fail-closed) on `/resume/:id/download`; (c) ✅ scripted — [`infra/supabase/storage-buckets.sql`](../../infra/supabase/storage-buckets.sql); still must be APPLIED; (d) ✅ [`apps/api/Dockerfile`](../../apps/api/Dockerfile) installs WeasyPrint with a build-time `--version` gate | **Paying down** — all four addressed in-repo. **Staging enablement readied (2026-06-15):** the private bucket is now declared in [`supabase/config.toml`](../../supabase/config.toml) (`public=false`, CLI/config source of truth, governs local), the remote apply + verify + ordered staging-flip runbook is in [`storage-buckets.md`](../../infra/supabase/storage-buckets.md) (`supabase db execute --linked --file storage-buckets.sql`), the exact staging env is templated in [`apps/api/.env.staging.example`](../../apps/api/.env.staging.example), and the live signed-URL security proof (happy path + anon-denied + expiry) is an executable, credential-gated test ([`resume-signed-url.e2e.test.ts`](../../tests/e2e/resume-signed-url.e2e.test.ts), skips without creds). **Bucket live-verified (2026-06-15):** `worker-resumes` created PRIVATE; the signed-URL suite passed 5/5 against the real project (private bucket, signed happy-path 200, both anon routes denied, 1s expiry) — R13 gate-2 cleared. Remaining: per-IP key is per-egress-IP until Express `trust proxy` is set (TD25 below); and the per-environment `RESUME_RENDER_ENABLED=true` app-flip is operator-run with that env's creds. ([ADR-0007](../decisions/0007-resume-render-node-boundary.md), R13) |
| TD24a | **Per-trade resume content is curated static copy, not from the questionnaire** — `trade-content.ts` covers 15 Phase-1 trades; `controllers`/`education`/`certifications` are still empty in the DraftProfile snapshot, and `responsibilities` are TRADE-level (not the worker's own achievements) | Deterministic, reviewed copy (no LLM) is the right Phase-1 source; richer per-worker content needs the questionnaire output threaded into the snapshot | Thread the rich questionnaire output (controllers/education/certs) into the resume snapshot; expand trades as the taxonomy grows | **Built** — content rows + selector + renderer wiring + tests. **Interview-kit content now covers all 15 alpha trades (2026-06-15)** — 9 missing kits drafted (`cnc_vmc_setter`, `cnc_programmer`, `vmc_programmer`, `solidworks_designer`, `autocad_draftsman`, `tool_room_technician`, `machine_operator`, `assembly_technician`, `fitter`); `REQUIRED_KIT_TRADE_KEYS` expanded to 15, tests green. So both content surfaces (resume + interview-kit) now cover the full alpha set. **DRAFTED, PENDING RVM RATIFICATION** — green tests prove presence/shape, not trade accuracy. RVM content review (CNC/VMC accuracy, realistic questions, natural Hinglish, no fabricated specifics, correct safety) is a human gate tracked in [trade-content-ratification.md](./trade-content-ratification.md); two flags logged there (`machine_operator` kit intentionally generic; "AutoCAD Draftsman" spelling). Not "final/approved" until RVM ticks the per-role checklist |
| TD25 | **Per-IP rate cap is per-egress-IP behind a proxy** — Express `trust proxy` is not set, so `req.ip` is the LB/proxy IP, not the real client; the cap is a coarse backstop until then. IP is HMAC-hashed before any Redis key/log (no raw PII) | Deployment topology (LB/proxy) not finalized | Set `app.set('trust proxy', …)` to the real hop count + revisit cap sizing once the edge is known | Open |
| TD26 | **Interview-kit render-once uses STORAGE (not a DB row) as the source of truth** — existence is a `storage.objectExists` HEAD; two concurrent first-requests can both render+upload (idempotent overwrite, deterministic bytes, so safe) | Avoids a new table + RLS surface for deterministic per-trade content | If kits need per-kit metadata/state (analytics, soft-delete, A/B), introduce an `interview_kits` table | Open (Task 4) |
| TD28 | ~~**`LITELLM_*` env vars are misnamed**~~ — they fed no litellm client; #28/#29 moved the AI service to **direct** Gemini (httpx) + Claude (anthropic SDK), and the Node-side `LITELLM_*` were vestigial (consumed only by `config.test.ts`, never by `apps/api`/`apps/web`) | Kept during the dep swap to avoid churning TS config + `.env.example` + the runbook | Unify env names across config (TS + Python), `.env*`, and docs; keep a one-release deprecation alias | **Paid (2026-06-15, [ADR-0008](../decisions/0008-litellm-to-direct-providers.md))** — unified on the shipped AI-service scheme: Node `packages/config` uses `GEMINI_FLASH_API_KEY` (master gate) + optional `ANTHROPIC_API_KEY`; `LITELLM_BASE_URL` dropped; `LITELLM_API_KEY` retained as a **deprecated alias** (the gate accepts either; `config.test.ts` covers it). `.env.example`/`config.py` were already provider-named. Canon reconciled (CLAUDE.md §3, README, architecture overview, decisions/risks logs). The cumulative spend cap remains open as **TD27** |
| TD27 | ~~**AI service has no cumulative/daily spend cap**~~ — the per-call `AI_MAX_CALL_COST_INR` ceiling ([`router.py`](../../apps/ai-service/app/ai/router.py) enforces it pre-call → mock) is the only programmatic backstop. Nothing bounds spend *across* calls, and retries multiply real calls up to ~9× per stuck extraction job (BullMQ `attempts:3` [`queue.module.ts`](../../apps/api/src/queue/queue.module.ts) × AI-service `max_retries:2` for extraction [`model_config.py`](../../apps/ai-service/app/ai/model_config.py)); an abusive/looping `/profile/extract` client is unbounded on spend | Phase-1 shipped the per-call ceiling first; cumulative budget deferred. **Not** a privacy/fail-open issue — pseudonymization still gates every call (fail-closed); the exposure is purely **spend**. Surfaced by the security audit gating the real-LLM canonicalization enablement | Before PROD real calls (or any non-fixed-`CASES` / open-loop real run): add a process-level rolling daily INR counter in [`cost_tracker`](../../apps/ai-service/app/ai/cost_tracker.py) checked in `router.run` before the real call (mock-fallback when exceeded — same pattern as the per-call ceiling), and/or cut the retry multiplication. For staging now: keep `AI_REAL_CALL_TASKS=profile_extraction`, run only over the fixed gold `CASES`, and watch the Langfuse cost/alert dashboard | **Paid (2026-06-19; was paying down 2026-06-15, [ADR-0008](../decisions/0008-litellm-to-direct-providers.md))** — `cost_tracker.SpendLedger` now enforces a rolling **per-UTC-day** + **cumulative** INR cap AND a rolling **retry budget** in `AIRouter.run` BEFORE `providers.complete` (fail-closed → mock; `error_code` daily_cap_exceeded / cumulative_cap_exceeded / retry_budget_exhausted), plus an independent **kill-switch** (`AI_REAL_CALLS_KILL_SWITCH`, checked first in `real_calls_blocked_reason`). Observable via `GET /ai/spend`, `/health.spend`, and the additive PII-free `ai.spend_cap_exceeded` event. Env: `AI_MAX_DAILY_COST_INR`/`AI_MAX_TOTAL_COST_INR`/`AI_RETRY_BUDGET_PER_WINDOW`/`AI_RETRY_BUDGET_WINDOW_SECONDS`. **Update (2026-06-15):** added a **PER-USER daily cap** (`AI_MAX_USER_DAILY_COST_INR`, default **₹6/user/day**) — the primary user-facing budget, shared across profiling chat + extraction + resume, keyed by the opaque `worker_ref` (checked first in `would_exceed_spend`; `error_code=user_daily_cap_exceeded`); the process-wide caps remain the no-`worker_ref` backstop. A normal worker journey is ~₹0.5, so ₹6 is an abuse backstop. Per-user usage observable via `GET /ai/spend?user_ref=…`. **Real-LLM validation gate PASSED** (2026-06-15, staging, Claude Haiku primary, behind this cap): per-field aggregate **95% ≥ 90%**, 56/56 real calls clean, ₹14.95 total spend, 0 TD3 over-masking. **Paid — final sub-item (2026-06-19):** the shared **Redis** store landed. `cost_tracker.SpendLedger` is now a name-stable facade over a pluggable `SpendStore` — `InProcessSpendBackend` when `REDIS_URL` is unset (the dev/CI default; per-process caps) and `RedisSpendBackend` (`redis.asyncio` + Lua) when set — so the daily / cumulative / per-user (₹6) caps enforce **GLOBALLY across all Uvicorn workers** via an **atomic check-and-reserve → reconcile → refund** (`would_exceed_spend` reserves the worst-case in one Lua eval; the router `record_spend(reserved, actual)` refunds the difference on success and the whole reserve on failure/abort via try/finally — no path leaks or double-refunds). This **closes the bounded check-then-act overshoot** from the security review (cross-link [R6](./risks-register.md)). **Fail-closed:** `REDIS_URL` set but Redis unreachable ⇒ reserve returns `spend_store_unavailable` ⇒ real call blocked → mock (never an unbounded spend); refund/snapshot never raise. PII-free keys + values (INR / counts / UTC date / **opaque** `worker_ref` only): `aispend:daily:{UTC_DATE}` (TTL→next UTC midnight), `aispend:total` (no TTL), `aispend:user:{UTC_DATE}:{worker_ref}`, `aispend:users:{UTC_DATE}` (SET). The retry budget stays per-process (a per-worker circuit-breaker, not a money guardrail). Proven by `tests/test_spend_cap_redis.py` (exactly-K-of-N concurrent reserves can't exceed a cap, reconcile/refund, fail-closed-when-down, UTC-day key+TTL, PII-free keys, backend selection) + the updated `test_spend_cap.py`; ruff + full AI-service pytest green; dev/CI run with NO Redis (`fakeredis[lua]` for the Redis-path tests). **Deploy precondition:** provision Redis + set `REDIS_URL` **BEFORE** `AI_ENABLE_REAL_CALLS=true` (else multi-worker caps silently become per-worker) — see the [go/no-go](../ai/real-llm-flip-go-no-go.md) + [architecture-log](./architecture-log.md) (2026-06-19). `AI_ENABLE_REAL_CALLS` stays OFF (separate human gate). Owner DevOps + AI |
| TD29 | **Worker-app alpha flows incomplete** — the Flutter app implements login→consent→chat→profile→resume-**text** only. Missing for the alpha capstone: **(G1)** resume **PDF download** (the app never calls the TD24 signed-URL route, and `GET /resume/:id/download` is `InternalServiceGuard`-only — needs a worker-authenticated path); **(G2)** **voice note** flow ([`voice_note_placeholder_screen.dart`](../../apps/worker-app/lib/features/voice/voice_note_placeholder_screen.dart) is a placeholder; backend STT is wired, TD6); **(G3)** **interview-kit** screen (backend content drafted PR #34; no app UI). **Swipe** is **out of Phase-1** (Reach Engine, §8). | Phase-1 scaffold shipped the profiling→resume happy path; PDF download + voice + kit UI deferred | Build G1–G3 before the alpha device capstone re-runs. G1 closes part of per-worker authz (**TD4/R11/R13**): add a `WorkerAuthGuard` download path verifying `resume.workerId === worker.id`, then wire mobile. Then run [the device test plan](../qa/phase-1-alpha-device-capstone.md) on a real Android handset against staging (no flow marked "works" without device evidence). | **Open** — surfaced 2026-06-15 by the alpha device-capstone attempt (NO-GO: app gaps + no device run possible from CI). **Triaged** into an owned, gated fix-list ([alpha-capstone-fixlist.md](./alpha-capstone-fixlist.md), 2026-06-15): only **B1** (device-verify the core path) BLOCKS-ALPHA-NOW; G1 PDF download / G2 voice / G3 kit are **JUN-25-DEV-INHERITS**; swipe is OUT-OF-SCOPE. **Update (2026-06-15): G1 backend DONE** — `GET /resume/:id/download` is now worker-authenticated + ownership-checked (closes the download IDOR for TD4/R11/R13; security gate PASS). **Remaining G1 (mobile, Jun-25):** ApiClient session-token plumbing + a "Download PDF" action that opens the signed URL (needs a Flutter SDK to verify the analyze gate + any `url_launcher` dep). G2/G3 unchanged. **Update (2026-06-15, [ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) Stream C): the mobile-half auth plumbing is now DONE IN CODE** — the worker SESSION/BEARER token is threaded into the `ApiClient` (memory-only, never logged) for the swipe-to-apply flow, so the J1 mobile-half session-token gap is **closed in code** (the remaining J1 piece is the "Download PDF" action itself). The **"swipe" capstone gap is also closed in code** via the new swipe screen + feed/apply/skip calls. Both still need the **device verification** (the separate Jun-25 capstone step) — not "works" until run on a real handset. **Reconciled + sequenced (2026-06-16, SoT = [alpha-capstone-fixlist.md](./alpha-capstone-fixlist.md)):** original G1 split three ways — **G1a backend worker-auth download CLOSED** (commit 8314dfc; `WorkerAuthGuard` + ownership, emits `resume.downloaded`, security PASS, closes the download-IDOR of TD4/R11/R13), **G1b mobile bearer-token plumbing CLOSED** ([ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) Stream C), **swipe code CLOSED** (ADR-0009 C, Phase-2 Reach feed stays OUT-OF-SCOPE). **Remaining buckets:** **MUST-LAND-BEFORE-JUN-25** = **B1** core-path device-verify on a real handset against staging (owner **qa-engineer**, target **2026-06-20**) — swipe device-verify folds into the same session. **ANDROID-DEV-INHERITS-DAY-1** (do NOT block the cut) = **G1c** in-app Download-PDF (owner **mobile-engineer**, ~**Jun-27**), **G2** voice flow (mobile, ~**Jul-02**), **G3** interview-kit screen (mobile, RVM+product gated, ~**Jul-04**). **GO condition:** Alpha NO-GO → GO when, and ONLY when, **B1 is device-verified on a real handset (CI/emulator do NOT count) with the 3 evidence artifacts** (screenshots + staging `events` chain + clean logcat). Override: if product/RVM require the branded PDF in alpha, **G1c promotes into the GO set** (worker-auth route + URL never logged, same change). PII rule honored: nothing unsafe ships today (G1c/G2 not built) — the PII default attaches to B1's consent-gate + clean-logcat assertions (before-Jun-25) and binds G1c/G2 to ship their privacy control (worker-auth route / PRIVATE bucket + pseudonymization) in the SAME change when built. See the SoT for the full tables. Owner mobile + backend; sequencing owner product-manager. **Day-1-ready (2026-06-17):** the inheriting Android dev (arriving Jun-25) now has an execute-top-down onboarding punch-list ([android-dev-onboarding-punchlist.md](../qa/android-dev-onboarding-punchlist.md)) — <1hr Day-1 setup (pinned Flutter **3.27.4** per the CI gate, build→staging via `--dart-define`, runbook smoke test) + self-contained G1c/G2/G3 cards (exact files, repro, acceptance tied to the B1 runbook, the same-change PII control each carries) + the sequence (G1c→G2→G3) + done/out-of-scope + the GO line. **G3 RVM content gate CLEARED** (CEO-approved 2026-06-17, [rvm-followup-nudge.md](./rvm-followup-nudge.md)) → G3 now needs only a product scope-confirm. **B1 re-verified 2026-06-19** against current `main`: the device happy-path was re-traced after the #91 P0 auth+consent merge — the app is correctly wired (OTP→token, **consent POSTed before chat**, bearer on chat), so the flow still completes and the consent-gate is now **server-enforced** (strengthens B1's b.4 evidence). **B1 stays NO-GO** on the two unchanged blockers: **(1) staging not deployed — DevOps, the critical path; the Jun-20 target is AT RISK** until the `API_BASE_URL` is delivered, and **(2) the human handset run**. See [alpha-capstone-fixlist.md](./alpha-capstone-fixlist.md) + [b1-device-capstone-runbook.md](../qa/b1-device-capstone-runbook.md) |
| TD30 | **CORS is unconditionally open in the API** — [`main.ts`](../../apps/api/src/main.ts) calls `app.enableCors()` with no origin allow-list, so any origin is permitted in every environment incl. production | Phase-1 has no browser client needing cross-origin yet (the ops console is internal); left open behind an inline `// TODO: lock down origins` | Before any production/staging deploy that serves a browser client: restrict `enableCors()` to an env-driven origin allow-list, routing the dev-permissive branch through [`isDevEnv()`](../../packages/config/src/shared.ts) so it fails closed | **Open** — flagged 2026-06-15 by the R14 boot-gate security gate as a separate, pre-existing transport gap (out of R14 scope; [R14](./risks-register.md)). Owner Backend + DevOps |
| TD31 | **Alpha-trade enums are duplicated in `@badabhai/db`** — `TradeKey`, `SkipReason`, and `SourceSurface` are mirrored as local TS unions in `@badabhai/db` (`jobs.trade_key`, `applications.reason`/`source_surface`) because the package cannot depend *upward* on `apps/api` or `@badabhai/event-schema` (dependency direction). Source of truth = `REQUIRED_TRADE_KEYS` in `apps/api` / the `application.*` payload enums | The DB schema needs the `$type<...>()` tags but can't import them from a higher layer; mirroring with "keep in sync with apps/api / payloads.ts" comments was the leanest option for the [ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) alpha | Extract a shared `taxonomy/enums` package that both `@badabhai/db` and `apps/api` consume, so the 15-key trade list + skip-reason + source-surface enums have one source. Until then, change all copies together | **Open** — low-risk (the unions are typed + commented, and a drift would surface as a type error at the boundary, not a silent bug). Cross-link [Q10](./open-questions.md). Owner DB + Backend |
| TD32 | ~~**CLAUDE.md §4 table count/list was stale** — said "DB tables (10)" / "10 tables" in the repo map while the schema had grown to 14, then 16~~ | Doc lagged the schema; flagged in [ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) §7 as the doc-fix to do when the `jobs`/`applications` tables land | Update the count + table list in the same PR that lands the migration; keep "PII lives only in `workers`" | **Paid (2026-06-15, [ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md))** — CLAUDE.md §4 now reads "DB tables (16)" and the repo-map line reads "16 tables", with the full current list (… `jobs`, `applications`). The "PII lives only in `workers`" invariant was kept verbatim; no other CLAUDE.md section, invariant, or the §8 deferred list was touched. **Note (2026-06-15, [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) Stream A):** the repo is now **20 tables** (the 4 additive unlock tables `unlocks`/`payer_credits`/`credit_ledger`/`unlock_routing` landed in migration 0014); the CLAUDE.md §4 count/list re-bump rides a later DB doc-fix (kept out of this factual register update) |
| TD33 | **`PayerAuthGuard` is interim `InternalServiceGuard`** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §6.3/F-7, [threat model](../security/contact-unlock-threat-model.md) T7) — Contact Unlock Stream A ships its payer-facing routes behind `InternalServiceGuard` (fail-closed shared secret, no per-payer identity); `payer_id` is trusted from the request body only because the caller is the backend/ops secret-holder | No payer identity/authz seam exists yet (the gated #42 jobs-entity work hit the same gap); the shared-secret guard is the leanest fail-closed interim for a mock-credits alpha | **Build `PayerAuthGuard` (per-payer identity + authz to `payer_id`) before ANY client-facing payer surface** — replace the body `payer_id` with the authenticated payer identity; add a horizontal-authz test (payer A cannot unlock/reveal/read credits for payer B's `payer_id`). **Launch gate** — cross-link [R16](./risks-register.md) | **Open** — surfaced + accepted at [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) sign-off (2026-06-15) as the interim alpha posture; flagged non-negotiable (no production payer surface on the shared secret). **Paying down (2026-06-20, [ADR-0019](../decisions/0019-self-serve-payer-portal.md) Phase 1, PR `feat/r16-payer-auth-wiring`):** `PayerAuthGuard` is BUILT + wired into `AppModule`; the payer-self disclosure routes (`/payer/unlocks*`) now derive `payer_id` from the verified session — the body `payer_id` is **gone (XB-A)**, and reveal ownership is enforced at the `UnlockService` chokepoint (not-owned/unknown → identical neutral body, no 403 oracle). Adds the self-serve payer **login** (signup + email-OTP default / WhatsApp-mock / inert-Supabase channel seam, minted via `PayerSessionService`), the **XB-G** per-payer disclosure rate cap, **XB-H** auth hardening (no user-enumeration, per-IP + per-account OTP caps, signed/revocable/rolling session, dev-JWT fails closed in prod via `assertPayerAuthConfig`), and the PII-free `payer.*` event domain. **bb-security-review + independent security-engineer PASS** (XB-A…XB-H; only Low/Info residuals). MOCK + STAGING-ONLY — the ops `InternalServiceGuard` `/unlocks*` routes are unchanged (backward-compat, §2.8). **Still deferred (launch gates, NOT closed):** DB-enforced RLS (XL-A/[R16](./risks-register.md)), real payments (TD34), production DPDP/DPA copy — open external GA stays human-gated. Owner Security + Backend |
| TD34 | **Mock payment seam — no real credit-pack purchase** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D5, [threat model](../security/contact-unlock-threat-model.md) T6) — Stream A ships the `PaymentGateway` interface + a mock credit-ledger debit path (`PAYMENTS_ENABLE_REAL=false`); ops grant credits, an unlock debits one, no real money moves | Real payment keys/spend are a human-gated escalation (CLAUDE.md §7); the seam is built so a real gateway slots in behind the flag, mirroring `AI_ENABLE_REAL_CALLS` | **Wire real Razorpay credit-pack purchase (₹1000 → 10 profile-unlock credits, ₹2000 → 25) behind `PAYMENTS_ENABLE_REAL`, staging-first, with explicit human sign-off** — add the opaque `provider_charge_ref` to `credit_ledger` additively; keep `real_call: true` honest on the `payment.*` events when live. **Launch gate** — cross-link [R17](./risks-register.md) | **Open** — interim mock path accepted at [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) sign-off (2026-06-15); real gateway is a separate gated stream. Owner Backend + DevOps |
| TD35 | **No retention/erasure policy for routing tokens + the worker↔payer linkage** ([ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D3, [threat model](../security/contact-unlock-threat-model.md) T10-a/LC-3) — routing tokens expire with the unlock window (`expires_at`, ~14d) but there is no written policy to **purge** the expired `unlock_routing` mapping, no documented retention window for `unlocks`/credit rows, and no DPDP erasure/crypto-shred story for the worker↔payer linkage (`unlocks.worker_id`) | Stream A built the PII-free data model + the consent mechanism; the retention/erasure *policy* is a legal-track follow-up (the append-only `unlock.*`/`contact.*` events carry no PII, so they persist; the linkage is erasable via the existing worker crypto-shred, TD22) | **Write + implement the retention/erasure policy before any non-mock disclosure (launch):** purge expired `unlock_routing` records; document a retention window for `unlocks`/credit rows; reconcile the worker↔payer linkage erasure with the crypto-shred-the-worker model (TD22) and the append-only events spine. Lands with the DPDP legal track. **Launch gate** — cross-link [R19](./risks-register.md) | **Open** — named at [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) sign-off (2026-06-15). Owner Product + Security |
| TD36 | **Reach feed serving deferrals ([ADR-0011](../decisions/0011-reach-feed-serving.md))** — (a) **full-pool scoring per request** (View A scores the whole `worker_profiles` pool in-memory) does not scale to large pools / many concurrent payers — the chosen alpha shape because it makes sort-never-block *structural* (no `WHERE`); (b) **`secondaryRoleIds` is dormant** — the mapper returns `[]` (no taxonomy-adjacency lookup wired), so the engine's related-trade partial-credit path never fires on this serving path; (c) **`StubJobSource` is the only job source** until the ops job-postings entity merges — the swap to a real `JobPostingsJobSource` needs a `job_postings` → `JobSpec` mapper with its OWN no-PII projection test (employer name/contact dropped there — that mapper becomes the new PII boundary); (d) **View A emits N `feed.shown` per ops page-load** via `emitMany` (one per pooled worker) — fine at alpha sizes, but ops-driven impressions inflate the events spine (LEARN windows/dedupes downstream — which is why `feed.shown` is unkeyed). | Strictly-additive read-only serving over the unchanged RANK core; the scaling read-model + enrichment + real job source are deliberately deferred Phase-2 follow-ups | (a) Phase-2 precomputed/cached read model for the rank input (signals + opaque ids only, no new PII location, sort-never-block preserved); (b) wire trade-adjacency into the mapper when the taxonomy lands; (c) author + test the `job_postings`→`JobSpec` mapper on the `JOB_SOURCE` swap (no-PII projection test required); (d) revisit impression granularity when the worker app ships. Unauthenticated endpoints tracked as [R22](./risks-register.md). | **Paying down** — (c) **DONE 2026-06-17** ([ADR-0015](../decisions/0015-reach-feed-on-real-jobs.md)): the real `JobsTableJobSource` now serves the live ADR-0009 `jobs` entity via a faceless `JobSignalRow` projection + pure `jobSignalRowToJobSpec` mapper (the no-PII boundary, asserted by a projection test); `roleIds` reuse the authored trade→role bridge in `trade-content.ts`. NEW sub-item: the 10 non-machining trades have no worker-role match until the worker taxonomy widens past the 7-role set, and that bridge could move to `@badabhai/taxonomy` when a 2nd consumer appears. (a) full-pool scoring, (b) dormant `secondaryRoleIds`, (d) N `feed.shown`/page remain open. **Update (2026-06-20, ADR-0019 R22 / PR2):** the new payer-self reach view (`GET /payer/reach/jobs/:jobId/applicants`) REUSES this same full-pool serving (so (a) full-pool scoring + (d) N `feed.shown`/load now also apply on the payer surface) — bounded there by a per-payer reach rate cap; the Phase-2 precomputed read-model (a) would benefit both the ops and payer reach reads. Logged 2026-06-16 from the ADR-0011 build; renumbered from a draft TD33 (collided with [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md)'s TD33 on `main`). Owner Backend (+ DB for the read-model) |
| TD37 | **Job-posting alpha-slice deferrals ([ADR-0012](../decisions/0012-ops-job-postings-banded-stored-only.md))** — (a) **two job-shaped entities coexist** by design: ops `job_postings` (banded, stored-only) vs the ADR-0009 swipe `jobs` (seeded, coarse `trade_key`/`title`/`city`, no vacancy count) — different concerns, no FK/bridge; (b) **`feed.shown.job_id` has no source discriminator** — if `job_postings` ever feeds a `feed.shown` producer alongside the swipe feed, ops- vs swipe-sourced impressions are indistinguishable in the LEARN spine; (c) **API response-shape inconsistency** — `/job-postings` returns the raw camelCase Drizzle row while `/workers` hand-maps to snake_case; (d) **web PII heuristic duplicated** — `apps/web/.../lib/job-postings.ts` re-declares the `looksLikePii` regexes (the browser bundle can't import the zod-bearing validator) → can drift from [`@badabhai/validators`](../../packages/validators/src/index.ts). NOTE: #48 shipped `job_postings` WITHOUT the TD20 RLS lock; the lock + LOCKED_TABLES entry were added during the merge reconciliation (migration 0015). | Additive alpha slice; convergence/normalization deferred to avoid coupling to the parallel `jobs`/`applications` work | (a)/(b) if ops postings ever feed Reach or a `feed.shown` producer, add a source discriminator via a **versioned** payload bump (never in-place) + a new ADR; (c) pick one response convention repo-wide when an external consumer appears; (d) extract the bare regexes to a dependency-free shared module, or add a client↔server parity test | **Open** — logged 2026-06-16 from the ADR-0012 build (principal-engineer **D2** + code-review **W1/N3** + security **NIT-1**). Unguarded ops endpoints + client-supplied `created_by` tracked as [R23](./risks-register.md)/TD4. Renumbered TD34→TD37 at merge (collided with [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md)'s TD34); migration regenerated against `main` as **0015**. Owner Backend + DevOps |
| TD38 | **`jobs.applicants_received` undercounts a skip→apply flip** — the counter bumps only on a genuine first INSERT with `action='applied'` (`upsertDecision` returns `inserted` via `(xmax = 0)`; `apply()` gates on it). A worker who first **skips** a job and **later applies** lands on the ON CONFLICT DO UPDATE path (`inserted=false`), so the apply is NOT counted; conversely an apply→skip flip never decrements (the counter is a monotonic count of received applies). | Deliberate alpha simplification — a single atomic `+1` keeps the increment race-safe with no transaction/advisory lock; the per-tap truth still lives in the `application.submitted` events spine | If exact apply-conversion accounting is needed: either derive the count from the `application.submitted` events, or change the increment gate to fire on an action **transition** into `applied` (prev≠applied) inside a per-(worker,job) guarded tx. Until then the counter is "first-apply rollup", not "current-applied set". | **Open** — logged 2026-06-16 from the T3 build ([ADR-0009](../decisions/0009-alpha-swipe-to-apply-seeded-jobs.md) applies-counter). Covered by unit tests (skip→apply no-increment + apply→skip no-decrement asserted). Owner Backend |
| TD39 | **Agency Referral Funnel + Payouts — PARKED Phase-2 fast-follow (not built)** — agencies refer candidates; a conversion inside a **90-day** attribution window earns a payout (**25%** share / **₹500**, **KYC required**). Needs a new attribution record, a payout (money-OUT) ledger, and an agency-KYC PII surface. | Deferred scope (CLAUDE.md §8) — real outbound money + KYC are human/legal gated; captured now so it's schedulable. Alpha ships WITHOUT it. | **UN-DEFER TRIGGER (build only when ALL true):** real inbound payments shipped (**TD34** closed) + real payer/agency identity (**TD33** closed); product ratifies the attribution/payout model; **legal + DPDP sign-off** on agency KYC; a human authorizes real payouts. Spec: [phase-2-agency-referral-payouts.md](../sprint-plans/phase-2-agency-referral-payouts.md). Depends on [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) (unlock/credit ledger), [ADR-0013](../decisions/0013-monetization-and-config-driven-pricing-engine.md). | **Deferred (parked 2026-06-17)** — capture-only, no code/schema. Owner Product + Backend + Security/Legal |
| TD40 | **Seeding / Credit Grants — PARKED Phase-2 fast-follow; assisted-hiring is a STUB in alpha** — grant credits without a purchase (promo/trial/assisted-hiring). The `credit_ledger` `grant` reason already exists ([schema.ts](../../packages/db/src/schema.ts) `CreditReason`); the grant **flow + authz + policy + audit** do NOT. | Deferred scope (CLAUDE.md §8) — a granting surface mints money-equivalent value, so it needs real grantor identity + abuse/audit controls first. Alpha's only crediting path is the manual ops **MOCK top-up** (TD34); **assisted-hiring is not built (stub).** | **UN-DEFER TRIGGER (build only when ALL true):** **TD33** closed (real grantor identity + authz); product+security define the grant/seeding policy (who, caps, expiry, reasons) + abuse/audit controls; a concrete promo or assisted-hiring program is greenlit. Spec: [phase-2-seeding-credit-grants.md](../sprint-plans/phase-2-seeding-credit-grants.md). Cross-link [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md) §D5. | **Deferred (parked 2026-06-17)** — capture-only, no code/schema. Owner Product + Backend + Security |
| TD41 | **Two threat-model §6 *static* regression guards are absent (Contact Unlock, [ADR-0010](../decisions/0010-contact-unlock-and-reveal.md))** — (a) **BC-8** has no test asserting `UnlocksRepository` is imported only inside `apps/api/src/unlocks/` (the structural sole-writer property); (b) **BC-5** has no static "exactly one `decrypt(` site on the unlock path" assertion. Both controls are named in [threat-model](../security/contact-unlock-threat-model.md) §6 ("a test that must exist"). | The *runtime/behavioural* guarantees they back ARE covered: BC-8 by `UnlocksModule` not exporting the repo (`unlocks.module.boot.test.ts`) + a verified grep showing no outside importer; BC-5 by the sentinel-phone-absent + `decrypt`-called-once + provider-error-fail-closed unit tests + the e2e no-PII sweep. The *static* CI guards (which would catch a FUTURE second writer / second decrypt site before it could leak) are the missing piece. | Add (i) a grep/static test asserting no module outside `unlocks/` imports `UnlocksRepository`; (ii) a static test asserting `pii.decrypt(`/`PiiCryptoService.decrypt` appears at exactly one site in `unlocks/` (the reveal handler). Cheap, additive, test-only — convert convention into a CI gate. | **Paid (2026-06-19)** — both static guards now exist in [`unlocks-static-guards.test.ts`](../../apps/api/src/unlocks/unlocks-static-guards.test.ts): **BC-8** scans `apps/api/src` and asserts NO file outside `unlocks/` imports from `…unlocks.repository` (sole-writer); **BC-5** scans `unlocks/` and asserts exactly ONE `.decrypt(` site (the reveal handler). Source-text scans → fail at author time before a leak can ship; both green. Flagged 2026-06-17 by the bb-testing/bb-security re-verification of the MERGED Stream A. **Renumbered TD39→TD41 at merge** (collided with the parked fast-follows TD39/TD40). Owner Security + QA |
| TD42 | **Boost RANKING is deferred — the booster reorders nothing today (ADR-0013 "pricing/boost never rank"; ADR-0011/0012 defer it)** — `posting_boosts` (₹1200/2d, tier `all_candidates`) + the `job_posting.boosted` v1 event exist as DATA only; `packages/reach-engine/*` and `apps/api/src/reach/*` have zero boost logic, the `jobs` demand-side source has no boost column, and `jobSignalRowToJobSpec` maps no boost into the RANK core. So the boost↔floor invariant holds VACUOUSLY. | Boost ranking is a Phase-2 surface with its own ADR + threat surface; activating it now would couple monetization to ranking (a worker-trust risk) before the floor contract is pinned. | **The deferral is now TEST-GUARDED (2026-06-17, this row):** **BOOST-INERT** ([`reach.job-source.test.ts`](../../apps/api/src/reach/reach.job-source.test.ts)) locks that boost never reaches a `JobSpec`/the engine; the **FLOOR-INVARIANCE CONTRACT** ([`reach-engine.property.test.ts`](../../packages/reach-engine/src/reach-engine.property.test.ts)) is the spec any future boost reorder MUST satisfy (reorder-within-the-above-floor-band only; off-trade never hot; scores/pushEligible immutable; deterministic), with a floor-violating negative control proving teeth. **Payback trigger:** if/when a boost-ranking ADR is approved, the implementation MUST keep both test groups green (the FLOOR-INVARIANCE CONTRACT's pluggable hook becomes the real `applyBoost`'s conformance test) — never relax the floor to make boost "work". Cross-ref [ADR-0006](../decisions/0006-reach-foundation-rank-core.md) / [ADR-0013](../decisions/0013-monetization-and-config-driven-pricing-engine.md). | **Open (deferred, guarded)** — logged 2026-06-17 from the boost-integrity fast-follow (tests + docs only; no boost ranking implemented). Owner Backend (+ system-architect for the future ADR) |
| TD43 | **Per-payer hiring-capacity build deferrals ([ADR-0016](../decisions/0016-payer-hiring-capacity.md))** — **(a0) enforcement is INERT/SHADOW by default (ADR-0016 D5)** — `CAPACITY_ENFORCEMENT_ENABLED` (default **false**) gates whether `buyPlan` actually pauses an over-cap plan; default = shadow: the chokepoint still computes the would-pause under the per-payer advisory lock but leaves the plan `active`, recording a **PII-free log line + a `wouldPause` result flag** (NO spine event — `action.recorded` is worker-scoped and `posting_plan.paused` asserts a REAL pause). So in alpha a payer is never silently paused. The `wouldPause` boolean is returned over HTTP to the `InternalServiceGuard` caller (derived from the caller's OWN purchase — it leaks nothing about other payers); when `PayerAuthGuard` lands, re-confirm `wouldPause` exposure stays acceptable on a real payer-facing response (code-review N2). (a) the per-payer active-vacancy cap is **ADVISORY until `PayerAuthGuard` (LC-1)**: the capacity endpoint (`POST /payers/:payerId/capacity`) rides `InternalServiceGuard`, so `payer_id` is client-supplied and a shared-secret caller can spoof it — the cap is only as trustworthy as that secret (same posture as [TD33](#); no production payer surface on the shared secret) — **this advisory-cap item now ALSO gates when enforcement should be flipped ON** (enable `CAPACITY_ENFORCEMENT_ENABLED` only when `PayerAuthGuard` lands, so a spoofable `payer_id` can never silently pause an employer); (b) **"paused excludes from reach" is a NO-OP today** — reach serves the `jobs` entity, not `job_postings`, so a `paused` posting_plan has no reach effect; the exclusion only becomes real if a `job_postings`→reach bridge is ever built (a future ADR), at which point the bridge mapper MUST skip non-`active` plans; (c) **web does not render the `capacity` product kind** — `apps/web/src/lib/api.ts` `ProductView` / `pricing-view.ts` keep their own view-type union that omits `capacity`, so the ops Pricing screen's `else`-branch won't render the seeded `hiring_capacity` product honestly until the frontend adds it (typecheck unaffected); (d) **concurrent double-`buyCapacity` upsert idempotency is not e2e-asserted** — `upsertCapacity` uses a `GREATEST` guard + the per-payer advisory lock, but a concurrent two-purchase race is proven only at the unit/mocked level, not in the opt-in e2e; (e) **shadow would-pause decisions are LOG-ONLY (no spine event)** — until/unless an additive v1 `capacity.evaluated` event is added (a future decision per ADR-0016 D5), shadow analytics live only in logs + the `wouldPause` flag, not in the `events` spine. | Additive alpha build; enforcement is opt-in (D5) so alpha ships safely without a payer-auth seam; per-payer auth, a reach bridge, the web capacity UI, the extra concurrency proof, and the optional spine-shadow event are deliberate Phase-2 follow-ups. The cap-compute, pause (when enforced), and auto-resume are fully built + atomic (advisory-lock e2e in `tests/e2e/payer-capacity.e2e.test.ts`). | (a0)/(a) build `PayerAuthGuard` before any client-facing payer surface (shared with TD33), then flip `CAPACITY_ENFORCEMENT_ENABLED=true` as a deliberate product gate; (b) author + test the `job_postings`→`JobSpec` reach mapper to skip non-active plans IF that bridge is decided; (c) frontend-engineer extends the web `ProductView` union + `toProductTierRows` to render `capacity`; (d) add a concurrent-double-buyCapacity case to the opt-in e2e when convenient; (e) add ONE additive v1 `capacity.evaluated` event (its own ADR) IF spine-level shadow analytics are wanted. | **Open** — logged 2026-06-17 from the ADR-0016 build (additive; mock payments only; faceless; **enforcement inert by default, D5**). Owner Backend (+ Security for PayerAuthGuard, Frontend for the web kind) |

| TD44 | **API hot-path load baseline not yet recorded ([docs/perf/api-hot-paths.md](../perf/api-hot-paths.md))** — the P3 perf pass landed the index + cap fixes (jobs/job_postings indexes migration 0022; `OPS_LIST_CAP` on the ops list reads; `CHAT_HISTORY_MAX` on the chat history read) and collaborator-level N+1/query-count guards (feed/reach `emitMany`-once, chat-loop one-history-read-per-turn). What is NOT done: an actual **recorded** load/micro-benchmark baseline (autocannon/k6 numbers — p50/p99, req/s) and `EXPLAIN ANALYZE` output proving the new indexes are used. | The local dev/CI box has **no Docker, no running Postgres, and no autocannon/k6** — a real baseline needs a seeded staging DB + a load tool, so it can't be produced in-repo without fabricating numbers (avoided deliberately). The fixes themselves are verified by the deterministic suite; only the empirical baseline is outstanding. | Run the baseline against a seeded target per `docs/perf/api-hot-paths.md` (`pnpm db:up && pnpm db:migrate`, seed, `pnpm --filter @badabhai/api dev`, `autocannon` the feed + ops reads; capture p50/p99 + `EXPLAIN ANALYZE` showing `Index Scan using jobs_status_created_at_idx`). Also (perf-engineer flag): true **DB-query-count** assertions (instrumented driver) would harden the N+1 guards beyond the current collaborator-count proxies. | **Open** — logged 2026-06-18 from the apps/api audit verification (perf domain NOT-FINISHED on the baseline criterion; indexes/caps done). Owner performance-engineer + Backend |
| TD45 | **PACE adjacent-trade leg is GATED + area-widen needs coordinate enrichment ([ADR-0021](../decisions/0021-pace-supply-widening-and-ops-alert.md))** — PACE ships the AREA-widen + OPS-ALERT legs; the **adjacent-trade** widen leg is built but inert behind `PACE_ADJACENCY_ENABLED` (default off) because there is **no ratified `ADJACENT_ROLES` map** (taxonomy has none; hospitality declined one; industrial is draft). Separately, the live `jobs` projection has **no centroid** (city-slug distance), so raising `maxTravelKm` only widens supply where coordinates exist — on the city-slug alpha path area-widen is a structural no-op (PACE then escalates to the ops alert). | Wiring a draft/guessed adjacency map would silently mis-target supply (a product judgement, not engineering — escalated); the reach engine is already adjacency-ready (`secondaryRoleIds` @0.6), so only the DATA is missing. Coordinate enrichment is a deferred reach follow-up (TD36a read-model family). | (1) Ratify an industrial adjacency map → wire `ADJACENT_ROLES` into `@badabhai/taxonomy` + `reach.mappers`, flip `PACE_ADJACENCY_ENABLED`, add the below-on-trade correctness test ([escalation](./pace-adjacency-ratification-escalation.md), [Q13](./open-questions.md)). (2) Enrich job/worker centroids so area-widen widens the city-slug pool. | **Open** — logged 2026-06-19 with the ADR-0021 build (the gated leg + the city-slug area limitation are accepted alpha posture; AREA-widen + OPS-ALERT shipped). Owner Product (adjacency ratification) + Backend/DB (wiring + centroids) |
| TD46 | **§3A `≤₹4/profile` AI cost guard is NOT enforced — held at ₹10/call** — `Settings.ai_max_call_cost_inr = 10.0` is a per-**call** ceiling, not a per-**profile** cumulative budget (cumulative is deferred per `router.py`), so a multi-call profile can exceed the §3A ≤₹4/profile lock. | Maintainer decision (2026-06-20): **keep ₹10 for now** rather than lower to ₹4 — avoids over-tightening the guard before real-call volume/cost is observed. Diverges from the §3A lock deliberately, not silently. | Lower `ai_max_call_cost_inr` toward ₹4 **and** add a per-profile cumulative budget (sum chat+extraction+resume per `worker_ref`) once real-call cost data exists; coordinate with the AI-routing/spend owner (Divyanshu). | **Open** — logged 2026-06-20 (maintainer "keep ₹10 for now"). Owner AI/Divyanshu |
| TD47 | **No first-class ops "modify pricing / AI cost cap" control** — catalog prices (`@badabhai/pricing`, `credit-packs.ts`) and the AI cost guard (`ai_max_call_cost_inr`) are config/env values: changeable today only via a config edit + deploy, not by ops at runtime. | Maintainer ask (2026-06-20): "later give us control to modify it." The values are already config-driven (no logic change needed to retune); a runtime admin surface is new scope, not part of the §3A reconciliation. | Build an ops-only "pricing & limits" modify control (guarded endpoint/UI writing a config row or env override) so prices + the cost cap can change without a deploy; treat as a deferred feature, not alpha. | **Open** — logged 2026-06-20 (deferred follow-up). Owner Product + Backend |
| TD48 | **The consent-gated agency attribution seam is built but INERT, and the worker-app-facing acceptance leg is unbuilt** ([ADR-0022](../decisions/0022-agency-supply-portal.md), #127) — `AgencyService.attributeWorkerToInvite` is an INTERNAL seam that NO-OPs unless an active `worker_consents` row exists (`findLatestByWorker` + `revokedAt IS NULL`). It is EXPORTED but currently has NO caller, so **no agency referral attribution occurs today** (`agency_invites.invited_worker_id` is never set, `agency_invite.accepted` is never emitted). **Two-client gap:** the agency demand backend is fully consumed by the **web** payer-web (agency = `payers.role='agent'` → `/payer/agency/*`), but there is **no PUBLIC worker-facing endpoint to accept/click an `agency_invites` code** — `agency_invites` is its own code namespace, separate from the ADR-0020 worker `invites` table + public `POST /invites/:code/click`, and the Flutter **worker-app** has no agency/invite surface. So the worker leg of the agency loop (worker opens an agency invite link → consents → gets attributed) has no contract the app can follow yet. | Shipped fail-safe (exported but unwired) so no attribution can attach before the worker→invite linkage product model is ratified — satisfying the ADR-0022 Appendix-C consent-before-attribution build-blocker by construction (no live write path to mis-order). The backend is the **source of truth** both clients follow; the worker-facing leg is deliberately deferred (consent-gated), not coupled to either frontend. | **UN-DEFER TRIGGER:** product ratifies the worker-side invite-attribution linkage model; then (1) expose a faceless PUBLIC worker-facing agency-invite acceptance path (or fold agency codes into the ADR-0020 invite/consent flow) so the Flutter app has a contract to follow, (2) wire `attributeWorkerToInvite` into the worker consent-accept flow, and (3) add the end-to-end test (attribution attaches ONLY after an active `consent.accepted`, never before/independent of it). | **Open** — logged 2026-06-22 with the ADR-0022 backend demand-slice build (#127; built INERT, no caller; worker-app leg unbuilt). Owner Backend |
| TD49 | **`db:verify:demand` rides the OPS surface — it will `401` when the R16/LC-1 payer-auth retrofit lands** — the BUG-2 demand-loop verifier ([`verify-demand.ts`](../../packages/db/src/verify-demand.ts)) drives the loop over `POST /job-postings/:id/plan` (unguarded) + `GET /reach/jobs/:jobId/applicants` (unguarded) + `POST /unlocks` / `POST /unlocks/:unlockId/reveal` (`InternalServiceGuard` + body `payer_id`). On current `main` this PASSes **as-is** (4-agent grounding 2026-06-23: all four routes unchanged; `forwardPortNeeded=false`). The PayerAuthGuard retrofit on the **unmerged** `feat/r16-lc1-payer-auth-close` branch puts `PayerAuthGuard` on `POST /unlocks*`, so once it merges the verifier 401s on unlock+reveal. | Forward-porting NOW would (i) break the verifier against current `main` (routes still `InternalServiceGuard` + body `payer_id`) and (ii) still **cannot be a pure-payer loop** — `job_posting.purchased` is emitted ONLY by the unguarded `POST /job-postings/:id/plan` ([`posting-plans.service.ts`](../../apps/api/src/posting-plans/posting-plans.service.ts) `emitPurchased`); no `PayerAuthGuard` route emits it. So the deliberate choice is to keep the verifier as-is until R16 lands. | **When `feat/r16-lc1-payer-auth-close` merges:** forward-port `verify-demand` to payer-session auth for the unlock+reveal steps — payer email-OTP login → `Bearer` → `POST /payer/unlocks` + `/payer/unlocks/:id/reveal` ([`payer-unlocks.controller.ts`](../../apps/api/src/payer-portal/payer-unlocks.controller.ts)), drop body `payer_id` (XB-A); add a `payers` ACCOUNT row for `PAYER_ID` to [`seed-demand.ts`](../../packages/db/src/seed-demand.ts) (today seeds only `payer_credits`); **keep** the plan-purchase on the unguarded `/job-postings/:id/plan` (hybrid loop). See the *Forward-port trigger* section of [bug2-staging-demand-deploy-runbook.md](../ops/bug2-staging-demand-deploy-runbook.md). Cross-link **TD33** (the `PayerAuthGuard` build), [R16](./risks-register.md). | **Open (deferred, deliberate)** — logged 2026-06-23 from the adversarial grounding of the BUG-2 R16 forward-port request (4 parallel route/seed/skeptic audits agreed the port is unnecessary on `main` + infeasible as a pure-payer loop). Tooling (#105) + deploy scaffolding (#131) already on `main`; this is the only forward-looking follow-up. Owner Backend + DB |

> When you pay debt down, mark it **Paid** with the PR link and date — don't
> delete the row. When you take *new* debt, add a row in the same PR that creates it.
