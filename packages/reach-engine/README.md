# @badabhai/reach-engine

**PLACEHOLDER ONLY — not implemented in Phase 1.**

The Reach Engine (deterministic *reach → rank → pace → protect → learn*) is a
core Phase 2+ artifact. LLMs must never rank, reject, or decide matches — that is
this engine's job once built.

`scoreWorkerForJob()` exists to stabilize the package boundary and call site; it
**throws** `"Reach Engine not implemented in Phase 1"`. Do not add matching logic
here in Phase 1.
