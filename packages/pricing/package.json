{
  "name": "@badabhai/pricing",
  "version": "0.1.0",
  "private": true,
  "description": "Config-driven Pricing Engine (ADR-0013) — typed Zod catalog schema + the deterministic, fail-closed resolvePrice algorithm. Ops edit the catalog VALUES; this package owns the SHAPE + the math. Pure, PII-free, no LLM.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
