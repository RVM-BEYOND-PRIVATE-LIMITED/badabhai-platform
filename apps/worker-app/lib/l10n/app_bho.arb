{
  "@@locale": "bho",
  "@@x-note": "भोजपुरी / Bhojpuri — INTENTIONALLY EMPTY, for the same reason as app_mr.arb: no native speaker on this change, and guessed copy in front of a low-literacy worker is worse than falling back. gen-l10n fills every key from the template (app_hi.arb), so Bhojpuri renders Hindi. SECOND, SEPARATE BLOCKER — read before wiring this up: `bho` is NOT in flutter_localizations' kMaterialSupportedLanguages (verified against the pinned 3.35 SDK: generated_material_localizations.dart has zero 'bho' entries, while 'hi' and 'mr' are both present). Putting Locale('bho') into MaterialApp.supportedLocales and then selecting it means GlobalMaterialLocalizations.delegate reports isSupported=false, DefaultMaterialLocalizations only supports 'en' (material_localizations.dart: `isSupported(locale) => locale.languageCode == 'en'`), and the worker gets a hard 'No MaterialLocalizations found' assertion — a blank-screen crash, not a degraded experience. So lib/l10n/ui_locale.dart deliberately keeps `bho` OUT of the UI locale list and maps it to Hindi, while LocaleStore keeps storing and sending 'bho' as X-Locale so the SERVER side of the choice is unaffected. Shipping a real Bhojpuri UI needs either upstream flutter_localizations support or a local delegate that dresses Material/Widgets/Cupertino in Hindi for a 'bho' locale. That is #315 follow-up, not this pass."
}
