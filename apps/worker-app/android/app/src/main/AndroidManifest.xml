<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Voice-note capture (A2): mic access for the `record` plugin. Runtime-
         gated — the OS prompt fires on first use via AudioRecorder.hasPermission();
         a denial degrades honestly to "type karke bhejein" (never a crash). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Firebase Cloud Messaging (native): network + show notifications on Android 13+. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="badabhai_worker_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Disable Impeller: its Vulkan backend renders corrupted output (blue
             diagonal streaks) on some Android emulators/GPU drivers. Falls back to
             the Skia (OpenGL) renderer. Flutter defaults Impeller ON for Android
             since 3.27; re-evaluate when emulator Vulkan drivers stabilise. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- Firebase Cloud Messaging: receives console test pushes; shows foreground
             notifications. Background/killed notification-messages are shown by the OS. -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <!-- Channel id used for background/system-tray FCM notifications (created in FirebaseManager). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- ADR-0032 profile-photo crop (image_cropper → uCrop). uCrop ships its own
             AppCompat-themed activity that must be declared by the host app. Portrait-
             locked to match the app; the theme comes from the appcompat lib uCrop
             pulls in transitively. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Grants the PDF viewer per-file read access to downloads saved by the
             API<29 fallback (app-external Download dir). API 29+ saves via
             MediaStore and opens the content:// uri directly, bypassing this. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.downloads.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/download_file_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets the "Kholein" action resolve a PDF viewer for DOWNLOADED LOCAL
             files (resume + interview-kit) under the Android 11+ (API 30+)
             package-visibility rules. Downloads are fetched IN-APP now — no
             browser / https VIEW intent is ever launched (the signed url never
             leaves the app), so no https query is needed. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="application/pdf"/>
        </intent>
    </queries>
</manifest>
