{
  "name": "@badabhai/payer-web",
  "version": "0.1.0",
  "private": true,
  "description": "BadaBhai EXTERNAL self-serve payer portal (Next.js) — ADR-0019 Phase 1: MOCK + staging-only.",
  "scripts": {
    "dev": "next dev -p 3002",
    "build": "next build",
    "start": "next start -p 3002",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf .next .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@badabhai/config": "workspace:*",
    "@badabhai/pricing": "workspace:*",
    "@badabhai/validators": "workspace:*",
    "next": "^15.1.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "server-only": "^0.0.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "vitest": "^2.1.8"
  }
}
