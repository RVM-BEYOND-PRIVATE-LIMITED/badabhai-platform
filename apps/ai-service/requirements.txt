fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
# LiteLLM + Langfuse are intentionally NOT hard dependencies in Phase 1 (real LLM
# calls are disabled by default). For real mode, install the optional extras:
#   pip install -r requirements-ai.txt
