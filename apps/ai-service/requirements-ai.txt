# Optional dependencies for REAL AI mode (AI_ENABLE_REAL_CALLS=true).
# The PRIMARY real provider is Google AI Studio (Gemini), called DIRECTLY over
# REST with httpx (a base runtime dep in requirements.txt) — there is NO LiteLLM
# proxy/SDK. The FALLBACK provider is Anthropic (Claude Haiku), reached via the
# official ``anthropic`` SDK. Langfuse is for tracing. Mock mode runs with
# requirements.txt only. Install with:  pip install -r requirements-ai.txt
-r requirements.txt
langfuse>=2.0,<3.0
anthropic>=0.40,<1.0
